# Anytype — official image sources

Recorded before commit, per REQ-007 / AC-008. Every image below was fetched from a domain on the
approved list (`anytype.io`, `docs.anytype.io`, `github.com/anyproto`) on 2026-09-05. None required
a login; nothing behind a paywall or account was touched.

## Licence position

Anytype has no published press kit, media-usage licence, or brand-asset terms at the URLs checked
(`anytype.io/terms`, `anytype.io/press`, `anytype.io/brand` all 404; the docs FAQ mentions
"trademark" and "brand" only in passing, with no explicit reuse grant). These images are public
product documentation and marketing material Anytype itself serves to any visitor — that is a
signal they are meant to represent the product publicly, but it is not a stated grant to
redistribute copies. **Position: terms unclear.** Each file below is committed for internal,
non-commercial product-comparison reference inside this repository, sourced and attributed by URL,
not represented as freely licensed. If a stricter reading is wanted, the fix is to delete the five
files and keep only this citation list — nothing else in the packet depends on the bytes being
present.

## Images

| File | Shows | Source URL | Fetched |
|------|-------|------------|---------|
| `anytype-board-official.jpg` | Kanban/board view, docs illustration | https://docs.anytype.io/anytype/organize/views (image: `Docs Views Kanban.jpg`) | 2026-09-05 |
| `anytype-table-official.jpg` | Grid/table view, docs illustration | https://docs.anytype.io/anytype/organize/views (image: `Docs Views Grid.jpg`) | 2026-09-05 |
| `anytype-calendar-official.jpg` | Calendar view, docs illustration | https://docs.anytype.io/anytype/organize/views (image: `Docs Views Calendar.jpg`) | 2026-09-05 |
| `anytype-gallery-official.jpg` | Gallery view, docs illustration | https://docs.anytype.io/anytype/organize/views (image: `Docs Views Gallery.jpg`) | 2026-09-05 |
| `anytype-properties-official.jpg` | Object properties/field-editing panel, docs illustration | https://docs.anytype.io/anytype/organize/properties (image: `Docs Properties Use Panel.jpg`) | 2026-09-05 |

All five are served from GitBook's asset host (`*.gitbook.io`) as embedded in the two pages above;
the `docs.anytype.io/anytype/~gitbook/image?...` wrapper URL on each page resolves to that same file.

## Timeline / gantt — not applicable

Anytype has no timeline or gantt view. Confirmed via the product's own view documentation (which
lists List, Grid, Kanban, Calendar and Gallery only) and via the Anytype community's own feature-
request threads for a timeline/gantt view, which remain open requests, not shipped features, as of
2026-09-05. No official image exists to fetch, so this row is recorded as **not applicable** rather
than uncaptured.
