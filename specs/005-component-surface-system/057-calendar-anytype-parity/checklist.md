---
title: "Verification Checklist: Calendar Anytype Parity"
description: "The thresholds with the failing measurement recorded first, so a pass means the calendar actually changed rather than a check being added."
trigger_phrases:
  - "057 checklist"
  - "calendar anytype thresholds"
  - "calendar parity verification"
importance_tier: "critical"
contextType: "planning"
---
# Verification Checklist: Calendar Anytype Parity

<!-- SPECKIT_LEVEL: 3 -->
<!-- SPECKIT_TEMPLATE_SOURCE: checklist | v2.2 -->

---

<!-- ANCHOR:protocol -->
## VERIFICATION PROTOCOL

Read exit codes without a pipe — `cmd >/tmp/out.log 2>&1; echo $?`. A pipe makes `$?` the pipe's
status. A criterion closes on a number that was read, never on a command that was merely run.

### Criteria

One row per acceptance criterion, numbered to match `AC-0NN`. Desktop measurements are taken on the
real renderer at the production mount point; phone measurements on a 390x844 profile with a navbar
present — **and against no reference, which is why C7 counts labels rather than matches.** **T002
fills every `Today` cell that carries a mechanism rather than a figure.**

**Priority:** C1 through C4 and C8 are P0. C6, C7 and C9 are P1. C5 and C10 are the operator's.

| # | Criterion | Today | Target | Evidence |
|---|---|---|---|---|
| C1 | Nine anatomy elements trued (AC-001) | **Was 0 of 9** — `design-trueup.md` did not exist and the capture set was unread. **Now 9 of 9**, landed 2026-09-05: 28 sub-rows with a measurement and a capture filename, 9 **pixel read owed**, 2 labelled `047`-sourced. A4 and A6 established across 10 light + 10 dark: **0** non-background px below the grid rule (y 1010..1210, x 625..2140), **0** ink in the header band between the title and the `‹ Today ›` cluster (y 262..296, x 800..1990). The menu count corrected: **5 distinct menus in 24 files**, the day and item captures being byte-identical (MD5 `28d38b3a…`) | 9 of 9, each with a capture filename and a measurement or a labelled inference; A4 and A6's absences established across all twenty set captures | [x] |
| C2 | Month grid, day cell, event chip match (AC-002) | **Was 91 classes, 133 rules, none trued** (measured on `3407dab0`). **R1/R2 now closed 2026-09-06** (recaptured and read pixel-by-pixel on `screenshots/notion-clone/views/calendar-month-view-desktop-{light,dark}.png` after the `box-sizing: border-box` fix at `styles.css:17397`): the grid's 8 vertical rules now sit at device x 112/489/865/1243/1619/1997/2373/2751, a 16px CSS inset on **both** edges of the 80..2783 container (was 112..2815, left-only); column pitch is **377.0** device px, matching the weekday row's **377.3** (was 386.2); each weekday label's inset from its own column's right rule now reads a constant **11.0-12.0px CSS** (Sun 12.0, Mon 11.5, Tue 11.5, Wed 11.5, Thu 12.0, Fri 11.0, Sat 11.5), against the prior 15.5-43.5px drift. What already held, read at DPR 2 off the same images: horizontal rules at y 477/751/1025/1299/1573, a 137px pitch (136px row + 1px border); `#F7F7F7` light and `#1E1E1E` dark weekend columns 1 and 7; a today disc measuring exactly 52x48 device px of `#216DFA` (26x24 CSS); chips flat at a 20px pitch. Five values remain **pixel read owed** exactly as recorded. `tasks.md` T015 R3 (rule colour and today marker geometry) is now closed too — the week/day slot lines and current-time marker read the month grid's literal `#EBEBEB`/`#292929`/`#216DFA` rather than the theme's `--background-modifier-border`/`--db-current-time-color`, with that variable and every `.db-timeline-*` rule reading it left untouched (gantt confirmed unmoved by MD5 and a zero-line diff). The timed-event block's own per-event colour is deliberately left alone, ADR-002's still-open question. R4-R7 remain open | Each of the three matched to a measured value or deviating on a named accessibility ground **R4-R7 closed 2026-09-06 and the whole row re-measured at the landing, live in the capture harness's own Chrome as well as on the recaptured images.** Grid border box CSS 40..1392, content 56..1376, inset 16px on both edges. Weekday cells and day cells share the pitch exactly: both at 56 / 244.56 / 433.14 / 621.70 / 810.28 / 998.84 / 1187.42, width 188.57, delta 0.00. Day-number ink 12.5px CSS below the cell top at all seven columns. Chip flat and 20px on desktop, 44px on phone (its ink centre exactly 12 CSS px lower than desktop's in the phone capture). Week and day take the two-select header, the `#EBEBEB` slot lines and the `#216DFA` disc. Empty backlog renders no element. The timed block's per-event colour is still deliberately untouched and is now T017's, not this row's | [x] |
| C3 | Navigation matches (AC-003) | **Was 6 controls beside a one-string title, no today scroll** (measured on `cc5a7ff2`). **Now matched**: the title is two buttons (month, year), each opening the shared dropdown-menu listbox and checkmarking the current value; header padding is 27px/20px; the title reads 16px; the "today scroll" positions the current week at the viewport bottom after a Today jump. The scale control, mini-calendar button and invalid-events toggle stay in the header — kept as ours (the operator's scale ruling, and two affordances with no reference counterpart), so the header still carries 4 control groups rather than 3, named rather than silently kept. The nav-button box itself stays pixel-read-owed on desktop (only its glyph was ever observable); on phone it now floors at 44px, closing the gap the coarse-pointer rule's 28px left open | Matched, or each declined part carrying a written reason | [x] |
| C4 | Date-property picker matches (AC-004) | **Was 0 of 5 sub-elements present.** **Now 2 of 5**: the `Show icon` toggle exists, sized to 26x16px and coloured to the measured `#5A92FC`/`#6E9EFC` pair, wired to the chip's leading icon (`config.showRecordIcon`, pre-existing and already exposed in the calendar's own Appearance section — not new plumbing, only the size/colour and this row's disposition are); and the date field itself is chosen via the pre-existing "Event start date field" control in the same settings surface, which is this calendar's functional equivalent of a single `Date Property` row, extended to a start/end pair for multi-day spans the reference product's single field cannot express. **Not built**: the 358x298px layout-tile panel, the 224px/28px-row submenu with per-property icons and a checkmark, the `#EBEBEB` divider, and `+ Add Property` (already declined on product grounds — frontmatter keys are not a property registry). Recorded as a bounded partial, not a false Met. **T016 (2026-09-06) closed the toggle's own unverified half**: no calendar capture had ever exercised the real icon path (every harness bag stubbed `renderRecordIcon` to `() => null`); `constructed-calendar-month` now wires a real icon renderer and one bench row carries an icon token, so the leading icon is confirmed rendering rather than merely wired. **T009 (2026-09-06) closed the submenu's own measured geometry**: a new `db-calendar-date-field-dropdown` class scopes the measured 224px width and 28px row height to the start/end date-field dropdowns only (per-option icons and the selected checkmark already existed, unmodified, in the shared dropdown-menu component). The layout-tile panel stays out of scope (`053` owns the view switcher, named in `design-trueup.md` §A8) and `+ Add Property` stays declined | Matched to the capture **T009's own geometry half was found not to have landed and was repaired at the landing (2026-09-06).** Opened in the capture harness's Chrome at DPR 2, the panel measured **280px**, not 224 — `positionToolbarPopover` writes a flat 280px inline, which outranks any stylesheet rule — and its rows measured **29.39px**, not 28, because `min-height` cannot shrink content that already exceeds it. Both now read **224 x 28** with every other dropdown in the same popover at **280 x 30**. The row stays open on the two surfaces that were never built | [ ] |
| C5 | **OPERATOR** — the scale ruling (AC-005) | **3 scales shipped, 1 layout captured.** `updateCalendarScale?(scale: "month" \| "week" \| "day", anchorDateKey, label?)` at `src/views/calendar-renderer.ts:82`, with `db-calendar-scale-{button,control,menu,menu-chevron,menu-label,popover,segment}` — seven classes for the switch alone — plus a full week body (`db-calendar-week-*`, 22 classes) and `calendar-keyboard-navigation.test.ts`. Anytype's six set layouts are Grid, Gallery, List, Kanban, Calendar and Graph; no scale switch appears in `anytype-menu-set-layout-calendar-*` | ADR-002 status is not **Proposed**; the implementation follows whichever way it goes | [x] ruling recorded 2026-09-05 ~23:20, *"Keep week and day, styled to the month grid"*; ADR-002 **Accepted**. The *follows it* half went back open at ~04:45 when the operator answered ADR-002's last question — *"Flatten to chip ink"* — and closes here: `tasks.md` **T017** landed the flatten, measured at **0** device px of the three former per-event fills (`#DEEAF1`/`#E6EFEA`/`#F9E9D8`) and the former `#1E3A8A` accent bar across all four recaptured `calendar-week-time-grid-*` files. The red value belongs to the two light files rather than to all four — **138,411/9,873/8,336** with a 3,496 px bar on `-desktop-light`, **21,708/2,572/1,059** with the same bar on `-mobile-light`, and the dark pair's own `#253652`/`#1E3E2A`/`#53331C` tints under a `#BFDBFE` bar — and all eight colours read 0 afterwards on a second decode written independently of `pixel-hash.mjs`. Every other value the ruling names — the rule colour, the today marker, the weekend tint, the nav cluster, the two-select header — was already re-measured at the earlier landing and stands. **The flatten's one cost is recorded, not absorbed**: at a phone's default column width an overlap-column block has no room for its title, so with the fill gone it decodes as nothing — `tasks.md` T018, a width question ADR-002 puts outside its colour ruling |
| C6 | Unscheduled area dispositioned (AC-006) | **Was ours and unmatched.** A collapsible backlog drawer: `db-calendar-backlog`, `-header`, `-toggle`, `-list`, `-item`, `-empty` (`calendar-renderer.ts:160-163`, `backlogCollapsed` at `:123`). **Now unmatchable, measured**: all twenty read, **0** non-background px below the grid rule in any of them, so there is no counterpart to compare against. Kept as ours with the written argument in `design-trueup.md` §A4, and restyled to the month grid's measured values. **T015 R7 (2026-09-06) closed the remaining placement gap**: an empty drawer rendered its header and empty line at 85 CSS px above the grid anyway; `renderUnscheduledBacklog` now returns before creating any element when nothing is unscheduled, so the drawer matches the reference's "no surface here" exactly in that case, and only appears — restyled, as already landed — once there is something for it to hold | Matched, or kept as ours with a written argument, after all twenty set captures are read | [x] |
| C7 | Phone values labelled (AC-007) | **no reference exists, and no labelling convention is in force yet.** `screenshots/anytype/mobile/` contains **0** calendar captures of any kind; the iOS view-layout sheets are picker, gallery and kanban (`anytype-mobile-sheet-view-layout-{picker,gallery,kanban}-{light,dark}.png`), and none of the 104 `mobile/sheets/` captures is a calendar surface. iOS Anytype does not ship a calendar layout | Unlabelled phone-calendar values → **0**. Each carries **"design inferred from desktop"** and names its source capture **Closed 2026-09-06.** `git diff origin/main -- styles.css | grep '^+' | grep is-phone` returns exactly one new phone-scoped calendar selector this packet added, `.is-phone .db-calendar-month-segment`, and T008's entry labels it against the measured 20px desktop pitch and the 44px touch floor it takes instead. Unlabelled count: **0** | [x] |
| C8 | `044` grammar + `048` stacking hold (AC-008) | **Measured 2026-09-06 on `cc5a7ff2`.** `node tools/live/sheet-grammar.mjs >/tmp/sheet-grammar-t002-057.log 2>&1; echo $?` → **0**. **12** registered surfaces, each PASS: `sort-panel`, `filter-panel`, `add-view`, `record-detail`, `record-peek`, `column-width`, `settings`, `board-card-properties`, `owned-menu`, `date-picker`, `icon-picker`, `option-color-picker`. `grep -c '^sheet-grammar: stacked pair —' /tmp/sheet-grammar-t002-057.log` → **31**. Log ends `sheet-grammar: PASS — every registered surface satisfies all eight grammar columns...` | 12 and 31 still green, `node tools/live/sheet-grammar.mjs` exit 0 read from `$?`, after every leg **Re-read 2026-09-06 at the landing, with the premise corrected rather than the check waived.** The registry now holds **13** surfaces: the thirteenth is `confirm`, registered by `055-states-feedback-and-motion` while this packet was open. **057 registered none** — the day and chip context menu is built on the already-registered `owned-menu` primitive. `node tools/live/sheet-grammar.mjs`: 13 surfaces, **31** stacked pairs, 0 FAIL, **exit 0** read from `$?` | [x] |
| C9 | Gantt unmoved, guard tests unedited (AC-009) | **Baseline measured 2026-09-06 on `cc5a7ff2`.** `grep -o "pm-gantt[a-z-]*" src/views/calendar-timeline-renderer.ts styles.css \| sort -u \| wc -l` → **119** (2 are comment-prose artifacts, not classes: `pm-gantt` from `calendar-timeline-renderer.ts:2206`'s comment and `pm-gantt-` from `styles.css:18742`'s `pm-gantt-*` wildcard reference in a comment — **117** real class tokens). `calendar-timeline-renderer.ts` is still 4317 lines (`wc -l`). Gantt capture MD5s (`md5 -q screenshots/project-manager/reference-gantt-*.png`): `desktop-dark 6d513240524177f37214f79180eafe2b`, `desktop-light 663e35073f34865557baa559298a8cff`, `mobile-dark 2b34a46cc7c76b7d77b65b6065d910c2`, `mobile-light 49cd9797c718e1e34c73058393b51753`, `subtask-desktop-dark 2fc6accee9fe7b715dd87289adde8077`, `subtask-desktop-light 304c3e0bf2419e25eb88c693af683c64`, `subtask-mobile-dark 577319931331726b199596e02a10e7c4`, `subtask-mobile-light 8f4828c88e556f90a8fb7fe0eaf0e601`. `npx vitest run src/views/calendar-keyboard-navigation.test.ts src/views/calendar-search-placement.test.ts` → exit 0, **2 files / 16 tests passed** | Baseline unchanged; both guard tests green with `git diff --stat` → **0** lines **Re-verified 2026-09-06 on the landed, rebased tree.** `git diff --stat 932fa3a9..HEAD -- src/views/calendar-timeline-renderer.ts` and the same over both guard tests print nothing at all. `grep -o "pm-gantt[a-z-]*" src/views/calendar-timeline-renderer.ts styles.css | sort -u | wc -l` -> **119**, unchanged. All eight `screenshots/project-manager/reference-gantt-*.png` re-rendered in the full capture run and their MD5s are byte-identical to the eight recorded here | [x] |
| C10 | **OPERATOR** — the calendar reads as Anytype on device (AC-010) | not asked; the packet was opened 2026-09-05 ~22:45 and nothing has shipped | The operator's own read, desktop and phone, knowing the phone half was inferred | [ ] |
<!-- /ANCHOR:protocol -->

---

## The check that the rest are not theatre

C2, C5, C6 and C7 already carry figures read off `3407dab0` or off the capture folder. C1 and C4
carried the absence of a read and no longer do: T001 landed 2026-09-05 and each now carries the
measurement that replaced it, with the pre-read state kept beside it so a reader can see what moved.
C3, C8 and C9 carried a mechanism instead of a number; T002 turned them into figures read off
`cc5a7ff2` on 2026-09-06, before any leg is written.

**One row moved for a reason worth naming.** C1's `Today` cell recorded "24 calendar menu files
(6 menus x light/dark x clipped/full)". The file count is right and the menu count was not: the day
menu and the item menu are the **same capture**, byte-identical in all four pairs. Six menus in a
threshold nobody had opened would have had a later leg building two surfaces from one screen.

**One threshold was deliberately not written.** The obvious mirror of `056`'s headline —
"Project Manager classes 39 → 0" — is unavailable here: this renderer carries **zero** of them, so
the criterion would have read green on an untouched tree and certified nothing. `050`'s true-up
found **six** such false premises in its own criteria. Naming why a threshold is absent is cheaper
than discovering later that a green one was empty.
