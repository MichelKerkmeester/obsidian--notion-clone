# Workflow synthesis mirror — 076 sheet visual parity

This file is the root workflow-owned synthesis artifact. The canonical detailed report, including the complete 87-surface matrix, current and target depth grades, patch-ready clauses, design-system map, loop changes, and operator questions, is at research/research.md in this same lineage.

## Terminal record

- sessionId: fanout-luna-1789102713325-fbmgv2
- iterations: 5 of 5
- stopPolicy: max-iterations
- stopReason: maxIterationsReached
- convergence threshold: 0.05
- execution: inline cli-codex model gpt-5.6-luna; nested dispatch false

## Synthesis verdict

The 076 packet has 19 existing multi-phased children and 87 inventory surfaces. It needs a parent count reconciliation, proposed bridge ownership for contextual rows 8 and 30 and roadmap rows 70–88, universal L3/L3+ phase evidence, normalized Source metadata and D7/D9 rules, shared token/primitive landing, and observable D6 liveness/verdict/release contracts. The canonical report contains paste-ready text grounded in source paths and quoted lines.

## Required patch order

1. Reconcile the 19-child map and add COV-001 through COV-094.
2. Add stable row IDs, thresholds, RED/GREEN clauses, named light/dark/full-sheet captures, per-row judge expectations, and tree-hash remediation records to every child.
3. Normalize Source, Source reason, Evidence rung, Measurement kind, State, Theme, Capture IDs, lane clause, and rubric rows.
4. Rewrite D7 card targets to plain background/divider language and resolve picker depth with ADR-N; preserve the 012 Anytype board-field exception under ADR-P.
5. Land semantic tokens, frame/header/stack, rows/dividers, record/property primitives, sheet families, and board exceptions serially under D4.
6. Add D6 phase heartbeats, stale recovery, evidence-bearing verdicts, calibration, dependency-aware scheduling, durable receipts, and DONE-only release gating.

## Coverage matrix

The canonical report enumerates every inventory ID 1–87 and roadmap request 70–94 with child, task, and clause. No row is intentionally left without an existing or proposed owner; 020 owns contextual record-peek/generic dropdown and 021 owns roadmap traceability pending operator approval.

## Depth grade per child per phase

The canonical report records the current L2/L3 matrix for children 001–019 and the L3+ target for every phase, plus child-specific clauses. Closure requires no cell below L3 and no visual evidence cell below L3+.

## Children to add

- 020 contextual-sheet-primitives — six phases for record-peek, generic dropdown, and shared contextual controls.
- 021 operator-ruling-traceability — six phases for roadmap request bridges, inventory release trace, and graph-count reconciliation.

## Design-system connection map

The canonical report maps semantic tokens, phone frame, handle/header, stack policy, row variants, dividers/sections, typography, property rows, hidden groups, toolbar primitives, semantic status/priority roles, and the scoped board exception layer to their current implementation seams and landing order.

## Loop-logic change list

Add phase heartbeats and stale timers; extend verdicts with tree/capture/rubric/lane evidence; calibrate the image judge against operator reads; require unchanged-tree DONE plus operator receipt; cap and schedule parallel work with a shared-CSS mutex and 018-to-019 dependency; persist transition receipts; retain hard-max-first stop semantics.

## Open questions for the operator

Choose physical versus parent-level children 020/021; choose ADR-N picker stacking policy; decide measured typography exceptions; approve bundled-surface representative judging; set phase timeout/heartbeat values; name calibration ownership; and decide whether graph receipts are durable lineage files or external receipts.
