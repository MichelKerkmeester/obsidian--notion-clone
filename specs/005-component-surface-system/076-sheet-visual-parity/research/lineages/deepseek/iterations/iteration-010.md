# Iteration 010 — CROSS-CUTTING traceability audit (rows → clauses → captures; claims → assertions)

**Gap class:** CROSS-CUTTING (terminal audit) · **Status:** complete · **newInfoRatio:** 0.63
**Findings file:** `findings/iter-10.md`

## Focus

The join the packet's own success criteria assume and no artifact performs: for every target a child
names, which assertion measures it and where that assertion lives — plus the corrections ledger and
the residual question set, gathered so the phase synthesis does not re-derive them.

## Headline findings

1. **X-1 — 119 DEFINE rows, 51 named clauses, 29 rows across seven children with none.**
   `003`–`019` carry 119 DEFINE table rows and 51 clause bullets (all of them in `003`–`012`);
   `013`–`017` and `019` carry 29 rows and **zero** clauses; `018` names “Lane clause, new” in its
   criteria without clause text. The unmatched rows are consistently the subjectively-judged ones
   (Sheet frame, Value affordance, Destructive scoping, Both themes). Clause ids resolve to the lane
   **for `002` only** — its `L1–L8` are the sole ids in `sheet-grammar.mjs`; `001`'s `L1–L10` exist as
   text and `071/002`'s “L8” collides in the same id space.
2. **X-2 — claim→assertion ledger.** Three rulings have mechanisms (D2b production mounts, D9
   stacking engine, D9 inset on landed sheets), two are structural (D2 full-sheet captures; D3's
   TBD discipline), and the rest — D7/no-cards, D9/44pt, D9/grab-handle, D8/DONE-gating, D6's graph —
   are asserted on whichever sheets landed or not at all. Proposed the packet-level `LC-1…LC-7` set
   binding every child, consolidating iterations 3/7/8's clauses rather than inventing new ones.
3. **X-3 — 21 residual questions, three owners, 10 operator-scope.** Only 10 of the 21 sit on the
   packet's own open-question lists (`goal.md` §3, OC-S1/S2, `005` §12); nine the audit raised are
   unowned, and all of them are inputs to a revision of `spec.md`/`plan.md`/D6/D9 that nobody has
   been asked to write. Proposed an answered-questions register in `decision-record.md` §10.
4. **X-4 — five corrections and four doc-truth defects.** DS-2→Y-1, LC-5→Y-3, C-3's narrowing, the
   full-sheet manifest correction, and Z-1's reachability sharpening; plus the still-open
   `SPECKIT_LEVEL 2`-vs-phase contract, the duplicate `roadmap.md` §4 row numbers, `002`'s stale
   “inset cards” wording, and the two disagreeing third-strike rules with `goal.md` still at
   `completion_pct 0`.

## Verdict

| Question | Answer |
|---|---|
| Every target asserted? | No — 51/119 rows, 29 with none |
| Packet-wide rulings packet-wide-asserted? | No — D7/D9 constraints are per-landed-sheet or prose |
| Release-note clause references resolvable? | Only `002`'s |
| All questions owned? | No — 10 operator-scope, 9 unowned by the packet's own lists |
| Corrections ready to carry? | Yes — 5 corrections + 4 mechanical repairs, each evidenced |

## What was ruled out

- **The clause sections join mechanically to the lane tool** — disproved; the join key exists only for
  `002`, so X-1's `Clause` column creates it.
- **The scaffold's `Source` column makes D9 traceable** — disproved; `018`/`019` carry criteria text,
  not clause text.

## Next focus

**Phase synthesis.** Ten iterations are complete at the cap (`stopPolicy: max-iterations` ×10). The
synthesis writes `research.md` (the ten iterations' findings, corrections and proposals, ordered for
an executor), the **coverage matrix** (operator request → child → clause → capture → rubric row), and
`convergence-report.md` with `stopReason: "maxIterationsReached"`, then closes the dashboard and the
strategy's machine-owned sections with the final ledger.
