---
title: "Verification Checklist: Modal and Sheet Componentization"
description: "The thresholds with the failing measurement recorded first, so a pass means a surface actually changed rather than a check being added."
trigger_phrases:
  - "051 checklist"
  - "shell primitive verification"
  - "modal componentization thresholds"
importance_tier: "critical"
contextType: "planning"
---
# Verification Checklist: Modal and Sheet Componentization

<!-- SPECKIT_LEVEL: 3 -->
<!-- SPECKIT_TEMPLATE_SOURCE: checklist | v2.2 -->

---

<!-- ANCHOR:protocol -->
## VERIFICATION PROTOCOL

Read exit codes without a pipe — `cmd >/tmp/out.log 2>&1; echo $?`. A pipe makes `$?` the pipe's
status. A criterion closes on a number that was read, never on a command that was merely run.

### Criteria

One row per acceptance criterion, numbered to match `AC-0NN`. Desktop measurements are taken on the
real renderer at the production mount point; phone measurements on a 390×844 profile with a navbar
present. **T002 fills every `Today` cell that carries a mechanism rather than a figure** — a "today"
cell written after the fix is a cell nobody can check against the tree that produced it.

**Priority:** C1-C5 and C8 are P0. C6, C7 and C9 are P1. C10 is the check that the rest are not
theatre and is P0. C11 is the operator's. **C12 and C13 are the parity retarget's (ADR-007)** —
C13 is P0, because it is the row that says whether the packet actually targets the reference or only
claims to; C12 is P1 and its value is `048`'s to own.

| # | Criterion | Today | Target | Evidence |
|---|---|---|---|---|
| C1 | One site decides chrome (AC-001) | **4** — `db-modal.ts:92-113`, `src/main.ts:3047`, `image-file-suggest-modal.ts:40`, `markdown-file-suggest-modal.ts:34`. **T002, 2026-09-05**: `rg -n "attachSheetChromeToModal\(" src/ --type ts` → 5 lines, 1 the definition (`mobile-bottom-sheet.ts:219`), 4 callers, matching exactly the four cited. **T005, 2026-09-05 (progress, not closed)**: `db-modal.ts` now delegates to `createSurfaceShell`, which is the one site deciding chrome for all 20 `DbModal` subclasses; re-run of the same command shows the definition, 3 calls from `surface-shell.ts`, and the 3 outlier callers unchanged — **4 raw call sites, 2 decision-making groups** (the shell; the three outliers, still direct and still blocked on `spec.md` §11's second open question). Not `Met`: the outliers are T010's, unstarted. **T010, 2026-09-06: still blocked, unchanged.** `spec.md`'s own reconciliation log states the question is unanswerable from any capture (no host to be native to), so it stays with the operator; the count is exactly what T005 left it at | **1**, or each survivor reasoned in the inventory | [ ] |
| C2 | Declared titles (AC-002) | **17 declared / 3 scraped, up from 0/20.** **T002, 2026-09-05**: `rg -n "extends DbModal\b" src/ --type ts` → 20; `rg -n "declaredTitle\|declareTitle\|sheetTitle\s*[:=]" src/views/ --type ts` → 0 matches (exit 1, no declared-title mechanism anywhere); `rg -n "getSheetTitle" src/ --type ts` → base at `db-modal.ts:82` plus one override at `create-linked-view-modal.ts:55`, both scrape-family. **T004/T005, 2026-09-05 (progress, not closed)**: the counted fallback exists — `surface-shell.ts`'s `resolveShellTitle` increments `getScrapeFallbackTitleUseCount()` every time it is asked for a title with none declared, asserted in `surface-shell.test.ts`. **T008/T009, 2026-09-06**: `db-modal.ts` now wires `title: this.getDeclaredTitle()` into every `createSurfaceShell` call; `rg -c "protected getDeclaredTitle\(\)" src/ --type ts` → 18 (17 overrides plus the base method returning `undefined`). `CsvMarkdownImportModal`, `CsvMarkdownExportModal` and the anonymous restore modal (`settings.ts:670`) are the three left scraping — none of the three is named by T008 or T009's file list, so they are an honest remainder rather than a miss. `create-linked-view-modal.ts`'s prior `getSheetTitle` override no longer exists (`rg -n "getSheetTitle" src/views/modals/create-linked-view-modal.ts"` → no matches) | 20 declared; the scrape counted, not assumed | [ ] |
| C3 | Sub-page replaces in place (AC-003) | **0 of 4 paths assertable** — no shell affordance exists. **T002, 2026-09-05**: `ls src/views/surface-shell.ts` → No such file or directory; `rg -n "createSurfaceShell\|replaceInPlace\|sub-page" src/ --type ts` → 0 matches (exit 1). **T006, 2026-09-05 (mechanism landed, still 0 of 4 in production)**: `surface-shell.ts` now exists with `pushSubPage`/`popSubPage` and a pure replace-in-place state machine, unit-tested (`surface-shell.test.ts`); `rg -n "createSurfaceShell\|pushSubPage\|popSubPage" src/ --type ts` → 6 matches, all inside `surface-shell.ts` and its own test — no producer calls it. The sub-page host (`view-config-panel-renderer.ts`) is outside this leg's file group, so all four paths stay unassertable in production | 4 of 4; parent box width and anchored edge `\|Δ\| ≤ 1px` (cross-axis not asserted, per the corrected threshold); picker over an undimmed parent; **2 pairs converted and no more** (`properties property type picker`, `add view property picker`); **stacked sheets at depth 3 → 0** (the depth cap, ADR-007) | [ ] |
| C4 | Inventory dispositions every surface (AC-004) | **Superseded by T001 — stale text corrected 2026-09-05.** The file now exists: `design-trueup.md`. **T002, 2026-09-05**: `grep -c "^\| [0-9]* \|" design-trueup.md` → 35 (20 subclasses + 3 non-`DbModal` outliers + 12 header sites); AC-004 in `acceptance-criteria.md` is already `Met` for this row — this cell was left saying "file does not exist" after T001 landed, and is corrected here rather than left contradicting AC-004 | every row, six cells, zero unknown | [x] |
| C5 | One exported confirm, 7/7 grammar (AC-005) | **0 exported, 0 of 7 asserted** — `openAndWait` (`modals/confirm-modal.ts:45`, entry `:98`) is correct but unexported as the family confirm. **T002, 2026-09-05**: `sed -n '35,45p;96,99p' src/views/modals/confirm-modal.ts` → `class ConfirmModal` (line 35) carries no `export` keyword; only `export function confirmWithModal` (line 97) is public, and it wraps `openAndWait()` rather than exporting it directly; `grep -n "confirm" tools/live/sheet-grammar.mjs` → the only hits are the `confirm over a sheet` and `import confirm dropdown chain` *stacking* pair names — no lane asserts the confirm sheet's own 7 header-grammar elements. **Fourth landing, 2026-09-06 — 1 exported, 7 of 7 asserted on a real mounted instance.** `src/views/confirm-sheet.ts` (new) exports `buildConfirmSheetBody`; `modals/confirm-modal.ts` now `export class ConfirmModal`, consuming the shared builder instead of hand-building its content a second time. `tools/live/sheet-grammar.mjs`'s `confirm` row imports `buildConfirmSheetBody` and `buildShellHeader` rather than mirroring their markup by hand, and now also runs in the title-centring probe (`TITLE_CENTERED_SURFACES`, previously excluded for lacking a real header shape to measure). `database-view.ts`/`embedded-database-renderer.ts`'s sort-conflict confirm and every destructive-confirm call site already named `confirmWithModal`, so `053` and `055` were already consumers of the wrapper; a census of hand-built confirm markup in `src/` (`rg -n "db-modal-actions" src/ --type ts`) → exactly one declaration (`confirm-sheet.ts`) and one consumer (`confirm-modal.ts`). Red-then-green observed directly: reverting the header's `buildHeader` wiring took `node tools/live/sheet-grammar.mjs` to exit 1 (`FAIL confirm — no .db-shell-header title to measure`); restoring it returned exit 0, 0 `FAIL` lines, confirmed twice. `npx tsc --noEmit` → 0; `npx vitest run` → 0, 1442 passing across 137 files | 1 path; 7 of 7 elements; `053` and `055` consume it | [x] |
| C6 | Shell geometry from named values (AC-006) **Third landing, 2026-09-06 — the shell path's own literal count is 0.** `surface-shell.ts` declares the phone close and the trailing chip as the named constants `SHELL_PHONE_CLOSE_PX` and `SHELL_TRAILING_CHIP_SIZE_PT`, and the stylesheet declares `--db-shell-edge-control-size: 44px` once for both edge controls; `rg '44px'` over the shell's own rules returns only that declaration. The frame shape adds two more named values rather than literals, `--db-radius-xl` and `--db-sheet-float-inset`, each used by rule and never repeated. | **per-surface literals; no shell geometry, red at ≥20 for the 360px width alone** — T002 records the literal count against the ADR-006-accepted form. **T002, 2026-09-05**: `ls src/views/surface-shell.ts` → absent; `rg -c "360px" styles.css` → 20 separate declarations, none reading a shared constant; `rg -n "border-radius: var\(--db-radius-lg\)" styles.css` → the 8px value is a design-system-wide token (`--db-radius-lg`, `styles.css:83`) reused ad hoc per surface, not a shell-owned geometry declaration. **T007, 2026-09-05 (the shell's own path only, not the packet-wide count)**: all seven counted properties, plus the rest of the measured set, are now named constants in `surface-shell.ts` (`SHELL_RADIUS_PX` through `SHELL_PHONE_CLOSE_PX`, locked by `surface-shell.test.ts`), and the new three-slot header CSS (`.db-shell-header`, `.db-shell-header-leading`, `.db-shell-back`) repeats the 44px close sizing as **three fresh literals** — `min-width: 44px` on the leading slot, `width`/`height: 44px` on the back control — rather than reading a shared value, which is a correction to what T007 first wrote here: the phone-close property's stylesheet count moved up by three, not down. Re-measured: `rg -c "360px" styles.css` → **21** (drifted by one since T002, unrelated to this leg — `git diff styles.css` carries no `360px` line). The desktop `panel`/condition/operator widths and the phone frame shapes stay per-surface literals outside this leg's file group; the count does not move packet-wide until the anchored-popover family migrates. **T011, 2026-09-06 — the three fresh literals are corrected.** `.db-shell-header-leading`'s `min-width` and `.db-shell-back`'s `width`/`height` now read a new shared token, `--db-shell-edge-control-size: 44px`, declared once beside the other shared tokens (`styles.css`, the `--db-radius-*` block) and also adopted by `.db-sheet-close` itself — `rg -n "44px" styles.css` inside the header block now shows exactly one occurrence (the token's own declaration) rather than four. This is the same "declared once, read everywhere" pattern `--db-radius-lg` already uses elsewhere in this file, not a new mechanism; it does not touch `surface-shell.ts`'s TypeScript constants, so the ADR-006 metric's literal wording ("outside `surface-shell.ts`'s named constants") is unmoved by it — the fix answers the landing verifier's plainer complaint, not this criterion's own denominator. Re-measured after the fix: `rg -c "360px" styles.css` → still 21, unaffected by a change scoped to the close/back/leading properties only. **Landing verification, 2026-09-06 — four defects fixed, each measured red then green.** (1) **Title centring**: `constructed-column-manager`'s "Properties" measured **27.67px off centre** with the leading slot back at its old fixed 44px and the trailing children ungrouped (`tools/live/sheet-grammar.mjs`'s new negative control, `db-shell-header-leading`/`db-shell-header-trailing` neutralised) — with the fix (a `db-shell-header-trailing` box grouping every trailing child, and the header laid out as a `1fr auto 1fr` grid on `is-phone` rather than a flex row) every title-bearing surface in the census measures **≤ 0.01px** off centre; `sort-panel`, `filter-panel`, `add-view`, `column-width`, `settings`, `board-card-properties`, `owned-menu`, `date-picker`, `icon-picker`, `option-color-picker` and `column-manager` all pass. (2) **Phone floating/flush frame (REQ-006 C10)**: implemented for the first time — `mobile-bottom-sheet.ts`'s `watchSheetFrameShape` measures a sheet's rendered height against the viewport on mount and on every `ResizeObserver` report, toggling `.db-sheet-floating`. Negative control: neutralising `.db-mobile-bottom-sheet.db-sheet-floating`'s own CSS measures `left: 0px, radius: 8px` (flush) on a surface still classified floating, proving the geometry rule — not merely the classifier — is load-bearing; the real rule restores `left: 8px, radius: 16px`. `sort-panel` (240px content) classifies floating at `8px`/`8px`/`8px` inset and `16px` radius all corners; `settings` (759.6px, capped at 90svh) classifies flush at `0`/`0`/`0` inset, `8px`/`0px` top/bottom-left radius. One pre-existing defect surfaced by the new bottom-corner-radius assertion and fixed in the same commit: the shared FLOATING PANELS desktop-popover rule (`.db-view-config-panel.db-surface` et al., §11117) declares an unscoped `border-radius: var(--db-radius-lg)` that out-specificities the flush base rule's un-`!important` `8px 8px 0 0`, rounding all four corners of `settings`'/`board-card-properties`' bottom edge instead of only the top two — corrected by adding `!important` to the base rule, matching the identical precedent already in this file for the sheet's own z-index. One further regression found and fixed: narrowing a floating sheet by 16px exposed `db-sort-rule-row`'s condition-row floors (`CONDITION_FIELD_FLOOR_PX`, tuned for the wider desktop popover) already running on the last few px of the sheet's own padding slack — `sort-panel`'s trailing "×" measured **+11px** past its own sheet edge on Chrome/WebKit; halved the padding-inline for `.db-sort-panel.db-mobile-bottom-sheet.db-sheet-floating` specifically (reclaiming the 16px the frame inset took), landing at **0px** overflow. (3) **The header-site census was short**: nine more raw `db-panel-header`/`db-panel-title` sites found beyond the twelve already migrated — `column-width.ts`, `chart-toolbar-renderer.ts` (four), `toolbar-renderer.ts` (two), `view-config-panel-renderer.ts`'s desktop-only second header, `column-manager-renderer.ts`'s desktop-only second header, `record-surface/record-header.ts`'s unconsumed phone builder — all now call `buildShellHeader`; `surface-shell.test.ts`'s consumer-file census extended from eleven files to fifteen. `calendar-toolbar-renderer.ts:89` and `calendar-timeline-toolbar-renderer.ts:69` are the two sites left, named rather than touched — `057`'s calendar leg owns those files while it is in flight. `node tools/live/sheet-grammar.mjs` and `npx vitest run` both exit **0** after every fix, and `npx tsc --noEmit` exits **0** | **Desktop** 8px / 16px / 8px / 28px / 360px / **288px** condition surface / **232px** operator dropdown. **Phone floating** inset **8px ± 1** on three sides, radius **16px ± 1**. **Phone flush** 0 / 0 / 0, top corners only. **Both** handle 34 × 5pt, rows 50pt, header ≈70pt, plain-row divider **20pt ± 1**, close 44px. **Header** three slots, title centre within **± 1px**. **Primary action** full-width pill **≥ 44px** (target 50pt). **Trailing chip** **44 × 44px ± 1** (all ADR-007). Plus: **zero of the shell's seven properties (radius, horizontal padding, vertical padding, divider clearance, row height, panel width, phone close) declared as raw literals outside `surface-shell.ts`'s named constants** (`decision-record.md` ADR-006, Accepted 2026-09-05 ~18:20, restating the count from a not-yet-existing shell path to today's scattered occurrences) | [ ] |
| C7 | Motion tokens + reduced motion (AC-007) | **per-surface literals** — T002 records the count. **T002, 2026-09-05**: `rg -n "\-\-db-sheet-enter" styles.css` → one shared var, `260ms` (`styles.css:121`), used only for sheet entrance (`ease-out`) with no matching exit/`ease-in` token; `rg -n "animation:.*ms\|transition:.*transform.*ms" styles.css` (excluding shimmer/shake) → `120ms` (`.db-overlay-enter`, menus/popovers, `:175`), `180ms` (`.is-stack-parent` scale-back, `:255`), `260ms` (sheet entrance), each a distinct per-surface-class value, none matching the target 200ms/150ms. **T007, 2026-09-05 (named, not yet consumed)**: `SHELL_ENTER_MS`/`SHELL_EXIT_MS` now exist as constants in `surface-shell.ts`, locked at 200/150 by `surface-shell.test.ts`; no CSS transition or entrance call reads them yet, so every surface's actual motion is unchanged and the stylesheet's three per-surface-class values above still stand | enter 200ms `ease-out`, exit 150ms `ease-in`; reduced-motion holds | [ ] |
| C8 | `044` grammar and `048` stacking hold (AC-008 regression half) | conforming today — **12** surfaces, **31** pairs green; the migrations must not regress it. **T002, 2026-09-05**: `node tools/live/sheet-grammar.mjs` → exit 0; 12 `sheet-grammar: <surface>` blocks (`sort-panel`, `filter-panel`, `add-view`, `record-detail`, `record-peek`, `column-width`, `settings`, `board-card-properties`, `owned-menu`, `date-picker`, `icon-picker`, `option-color-picker`); 31 `sheet-grammar: stacked pair` blocks; 0 `FAIL` lines; final line `PASS — every registered surface satisfies all eight grammar columns … every registered parent-to-child row keeps one scrim …`. **T011/T012, 2026-09-06**: re-run after migrating eleven header call sites onto `buildShellHeader` — still exit 0, still 12 and 31 green; `hasSheetHeader`'s own predicate (`.db-panel-header` with a non-empty `.db-panel-title` and a `.db-sheet-close`) is unchanged by the migration, which only adds a leading sibling and a class no predicate reads. **Landing verification, 2026-09-06**: re-run again after the nine-site census correction, the title-centring fix and the C10 frame-shape implementation — `node tools/live/sheet-grammar.mjs` → exit **0**, still 12 surfaces and 31 pairs green, plus two new permanent rows (title centring across 11 surfaces with a negative control; the frame shape on `sort-panel`/`settings` with a negative control), both observed red before green | 12 and 31 still green after every leg | [ ] |
| C9 | PM board and gantt parity (parent D5) | baseline **not yet captured** — T003 records the `pixelHash` before the first shared-chrome commit. **T002, 2026-09-05**: `find . -iname "*051*baseline*" -not -path "*/node_modules/*"` → 0 results; no `pixelHash` reference exists yet under this packet — confirmed still red, T003's task, not measured further here. **T003, 2026-09-05 — baseline recorded**: `constructed-board`, `constructed-board-subtask`, `constructed-timeline`, `constructed-timeline-subtask`, `reference-kanban`, `reference-kanban-subtask`, `reference-gantt`, `reference-gantt-subtask` — 32 device/theme `pixelHash` values read from `screenshots/manifest.json` before this leg's first edit. Re-read after T004-T007 landed (full recapture, 554 entries): all 32 values hold unchanged. **Re-verified at the landing, 2026-09-05**, independently and against a main that moved five times during the pass: the branch was rebased onto `d00cf59c`, all 558 captures regenerated, and the working manifest diffed entry-by-entry against the base's — **0 of 558** `pixelHash` values differ, the 32 Project Manager entries included. One intermediate rebase did show a single mover, `constructed-table-footer-desktop-light.png`, and it was chased down rather than waved through: identical `layoutHash`, and with `origin/main`'s own `styles.css` restored byte for byte and this leg's block removed it recaptured to the same new hash rather than to the committed one, so it was the capturing environment. This base's own recapture has since absorbed it. **T008-T012, 2026-09-06**: recaptured a third time after the header migration and its CSS fix — the 32 board/gantt entries (`constructed-board`, `constructed-board-subtask`, `constructed-timeline`, `constructed-timeline-subtask`, `reference-kanban`, `reference-kanban-subtask`, `reference-gantt`, `reference-gantt-subtask`, each device/theme) all read `pixelHash`-identical to HEAD. The `constructed-board-card-properties-mobile-*` pair did move — that is the board variant of the settings sheet's Properties panel, a `sheet-grammar` `board-card-properties` row, not one of D5's protected board/gantt view captures, and it moved for the same reason every other migrated mobile surface did (the centred three-slot header) | `pixelHash`-identical, or an operator ruling | [ ] |
| C10 | Gate + replay + lane rows (AC-008) **Third landing, 2026-09-06 — re-run from the final state after four rebases** (onto the 052/054, 055, 053 and 057 landings in turn). `npx tsc --noEmit` → 0; `npx vitest run` → **1442** passing across 137 files; `npm run build` → 0; `node tools/screenshots/verify.mjs` → 0, **562** current; `node tools/lane/check-lane.mjs` → 0, the lane taken for this phase with a release naming its **44** moved captures; isolated `npm run gate </dev/null > ".gate-$$.log" 2>&1; echo $?` → **0**, 26 green 0 red, read from the file; `npm run replay` → 0, **28 of 28 held**; `node tools/live/evidence.mjs --check-all` → 0, 15 of 15 fresh. Two red-first controls ran inside `sheet-grammar`: the old two-slot header rule measures 27.67px off centre and the grid restores 0.01px, and neutralising the floating geometry takes the sheet to 0px/8px radius before the real rule restores 8px/16px. Two harness races were fixed at the producer on the way — the capture shutter now waits for the frame-shape classifier instead of racing its debounce, and the rebuild lane waits for the sheet to be resting instead of retrying its own assertion three times. | **not yet run for this phase**; the shell's rows do not exist. **T002, 2026-09-05**: `rg -n "shell" tools/gate.mjs` → the only hits are an unrelated comment ("a shell loop") and the `shell: false` spawn option — zero shell-deliverable check entries; `node tools/live/replay.mjs` → exit 0, `replay: PASS — all 28 results still hold`, `reversed: 0` (per `tools/live/replay.json`, reverted after reading — not a packet artifact). Full `npm run gate` (tsc/tests/lint/21 scanners) not run for this leg — out of T002's scope and would touch unrelated tracked stamp files; the shell-specific absence above is what this row asks for. **Run at the landing, 2026-09-05, after the rebase onto `d00cf59c`**: `npm run gate </dev/null > .gate-verify.log 2>&1; echo $?` → **0**, `PASS — 26 green, 0 red for a declared reason`; `npx tsc --noEmit` → 0; `npx vitest run` → 0, 1329 passing across 125 files; `npm run build` → 0 with no tracked diff; `node tools/screenshots/verify.mjs` → 0, 558 current. **Still unticked**, and the reason is the other half of the target: the gate carries no shell-deliverable row, because this leg added no lane — that is T012's own row, distinct from T012 as landed here (a regression check, not a new lane), and a gate green without a row for the thing built is exactly the theatre this row exists to catch. **T008-T012, 2026-09-06**: `npm run gate </dev/null > .gate-<pid>.log 2>&1; echo $?` (isolated log) → **0**, 26 green including `sheet-grammar` and `css-lane`; `npx tsc --noEmit` → 0; `npx vitest run` → 0, **1370** passing across 127 files; `npm run build` → 0; `node tools/screenshots/verify.mjs` → 0, 558 current; `node tools/live/evidence.mjs --check-all` → 0, 15 of 15 fresh. Still no permanent shell-deliverable lane row exists — that is T015's. **Second landing verification, 2026-09-06 — red before green, not merely re-run.** A fresh isolated `npm run gate </dev/null > ".gate-$$.log" 2>&1; echo $?` immediately after C10's stylesheet and TypeScript landed read **1** — `RED`: `placement` (22 assertions, later 15 after an interim fix), `sheet-rebuild` (3 cases), `evidence` (9 of 15 stale), `css-lane` (stylesheet moved, no release entry). `placement` and `sheet-rebuild` were real regressions, not stale process state: both lanes hardcoded "every phone sheet is flush" (a 0px floor inset, a full-viewport width) into their own assertions, which C10's floating shape correctly fails for any short surface — fixed by reading the sheet's resting inset directly (`db-sheet-floating` class, or `getComputedStyle(panel).bottom`) at each of the fifteen `verify-placement.mjs` sites plus one content fix (the record-detail floor stub grew from 6 fields to 20, so its own navbar-coverage claim stays about a realistically tall record). `sheet-rebuild.mjs` additionally uncovered a genuine timing defect the geometry fix alone did not close: a fixed-coordinate tap on a floating filter sheet's `+ Add` button after a background toolbar rebuild missed roughly one run in three while `mobile-bottom-sheet.ts`'s frame-shape classifier (`ResizeObserver`, debounced) was still settling under load — closed by moving the settle wait from a fixed 120ms/2-frame budget to 500ms wall-clock and retrying the whole open-rebuild-tap sequence up to three times, clean across 14 consecutive runs after. One more defect surfaced and fixed in the same pass: the floating shape's own drag-band width rule (`.db-sheet-floating .db-mobile-bottom-sheet-handle::before`) read `width: 100%` against the wrong containing block (the 36px handle, not the sheet), shrinking the drag target to the handle's own width on every floating sheet; corrected to `calc(100vw - 2 * var(--db-sheet-float-inset))`. Re-run after every fix: `npm run gate </dev/null > ".gate-$$.log" 2>&1; echo $?` → **0**, 26 green, confirmed twice in direct succession; `npx tsc --noEmit` → 0; `npx vitest run` → 0, **1378** passing across 127 files; `npm run build` → 0; `node tools/screenshots/verify.mjs` → 0, 558 current; `node tools/live/evidence.mjs --check-all` → 0, 15 of 15 fresh; `node tools/live/capture-device-parity.mjs` → 0. `css-lane` closed by a proper release entry (`tools/lane/css-lane.json`, `baselineHash` `2d6dda9a2873`, 47 reviewed captures) rather than an env-var bypass. **Fourth landing, 2026-09-06 — one more permanent row, the confirm row's fidelity fixed, both observed red then green.** `tools/live/sheet-grammar.mjs` §2e adds the edge-control-token row: `.db-sheet-close` measures **44 × 44px** on `sort-panel`; a negative control overriding `--db-shell-edge-control-size` on `.db-surface` (the class the token is actually declared under — a `:root` override was tried first and read unchanged, since a direct declaration on the panel element wins over anything merely inherited) moves the measurement to **60 × 60px**, then removing the override restores **44 × 44px**. Observed directly: the mis-scoped `:root` version read `FAIL overriding the token moves the close control (44.0x44.0)`, exit 1; the `.db-surface`-scoped fix reads two `PASS` lines, exit 0. Separately, the pre-existing `confirm` row (asserted since an earlier landing) now mounts through the real `buildConfirmSheetBody`/`buildShellHeader` primitives instead of hand-mirrored markup, and is newly included in the title-centring probe — see C5. Re-run after both fixes: `npm run gate </dev/null > ".gate-$$.log" 2>&1; echo $?` → **0**, 26 green; `npx tsc --noEmit` → 0; `npx vitest run` → 0, 1442 passing across 137 files; `npm run build` → 0; `node tools/live/sheet-grammar.mjs` → exit 0, 0 `FAIL` lines. **Still not `Met`**: "one row per deliverable" is the packet's full geometry/motion set (AC-006, AC-007) — the primary action pill, the trailing header chip and the motion timing band still carry no lane row | gate exit **0**, one row per deliverable, controls red→green; replay reversed **0** | [ ] |
| C11 | Operator device pass (AC-010) | **not asked yet** | the operator reads the family as one; **operator-only row, stays unticked until they say so** | [ ] |
| C12 | Scrim levels (AC-011) | **no scrim exists** — `design-system.md` §7: *"There is no sheet scrim … A scrim is new construction"*; `design-trueup.md` §6 C3 records it as the red. Not re-measured by the retarget: this is a statement about our tree, and T002 did not change | page under a first sheet **0.519 ± 0.02**; parent sheet under a stacked child **0.710 ± 0.02**; a stacked menu leaves its parent undimmed on desktop | [ ] |
| C13 | Parity by default, exceptions named (AC-012) | **18 undeclared declines**, counted at T001's state 2026-09-05 before ADR-007: 15 of the 35 `design-trueup.md` §5 decision cells recorded a measured Anytype value and then kept ours with no ground stated (rows 2, 3, 4, 5, 12, 15, 19, 21, 22, 23, 26, 27, 29, 33, 35), and 3 of the 10 §6 resolutions deferred, routed or declined one (C4, C7, C8). This is a document count, not a tree count — it is red against the *decisions*, which is what AC-012 gates | every §5 cell and §6 resolution opens **ADOPT** / **FLIPPED** / **HOLD** / **REFUSE** (post-retarget: §5 **16/7/12/0**, §6 **9/0/0/1**); `REFUSE` cells with no ADR-007 exception → **0**; exceptions on a ground other than WCAG 1.4.11, 1.4.3 or a 44px floor → **0**; exactly **3** exceptions plus **1** flagged hold | [ ] |

**C1, C3 and C5 are what a later reader will check first. C10 is the check that C1-C9 are not
theatre. C13 is the check that the packet is aimed at the reference rather than at itself. C11 is
the check that only the operator can pass.**

**None of C1-C10's `Today` figures moved for ADR-007.** They were measured against HEAD at
`de4783bb` and describe the tree; the parity retarget changed documents. C12 and C13 carry their own
reds, measured for this pass — C12's off `design-system.md` §7's own statement, C13's by counting
the decision cells.
<!-- /ANCHOR:protocol -->
