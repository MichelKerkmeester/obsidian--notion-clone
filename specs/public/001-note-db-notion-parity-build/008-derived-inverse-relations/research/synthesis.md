# Synthesis: Derived Inverse (Safe Two-Way) Relations
> One-line: ranked Notion-parity enrichment for this feature, synthesized by Grok 4.6 (xhigh-fast) from the phase's 10 research iterations. Evidence trail: research.md.

## Verdict
This is worth building now: Wave 4, effort M (~8h), value 4, and the fork already has the forward scan, display-only rollup precedent, and per-path write queue this design reuses. Headline recommendation: ship a **read-only derived inverse** in `src/data/RelationInverse.ts` (one stored many-to-one wikilink, inbound set computed in memory) and wire it through `RelationRollup.ts` + `RelationLinks.ts`; do **not** port Notion’s `dual_property` mirror. The single biggest risk is a naive two-way port that dual-enqueues `DataSource.writeQueues` and churns two iCloud notes; the single biggest open decision is whether SC-002 is satisfied by a Report-side `list`/`count` rollup over inbound edges (fits the locked 1–3 hunk budget) or whether the record-page inbound section in `RecordDetailPanel.ts` is allowed as an extra file.

## Ranked backlog
1. **Derived inverse fan-in index** — Notion shows related pages on the *target* via a second stored relation (`dual_property`); the fork only scans outbound from the source DB and would otherwise need a mirrored frontmatter property. Feasibility: **clear**. Files: create `src/data/RelationInverse.ts`. Effort: **M**. Depends on: phase `001-live-reports-rollups` (live forward relations + rollup scan). Citation: Notion relation type object `{ data_source_id, dual_property: { synced_property_id, synced_property_name } }` at [developers.notion.com/reference/property-object#relation](https://developers.notion.com/reference/property-object#relation); fork scan to invert at `src/data/RelationRollup.ts:24-88`.

2. **Rollup-over-inverse (`count` / `sum` / `avg` / `list` on the target DB)** — Notion rollups compute over the related-pages set on whichever side the rollup column lives; today `buildRelationRollups` only follows a *local* `RollupConfig.relationField` and writes `emptyRollupValue` when that key is missing (`src/data/RelationRollup.ts:62-66`). Feasibility: **clear**. Files: `src/data/RelationRollup.ts` (import `buildRelationInverse`; when `relationColumns.get(config.relationField)` is null, resolve a source relation on another DB whose `targetDatabaseId === sourceDatabase.id` and pass inbound `NoteRecord`s to existing `aggregateRollup` at `:92-129`). Effort: **S**. Depends on: item 1. Citation: fork `RollupConfig` at `src/data/types.ts:39-45`; Notion rollup display options (Show original / Count / Sum / Average) at [notion.com/help/relations-and-rollups](https://www.notion.com/help/relations-and-rollups).

3. **Read-only inverse chip contract** — Notion’s related column on the target is ordinary relation chips; the fork’s chip grammar already exists but only for stored frontmatter wikilinks. Feasibility: **clear**. Files: `src/data/RelationLinks.ts` (export a helper that maps inverse edges → `ParsedRelationLink` and reuses `getRelationDisplayLabel` at `:29-31`; strip `|` alias and `#` subpath with the existing parser at `:15-19`). Effort: **S**. Depends on: item 1. Citation: `src/data/RelationLinks.ts:1-31` plus renderer `src/views/RelationValueRenderer.ts:7-37`.

4. **Hide-when-empty inbound surface** — Notion offers `Always show` / `Hide when empty` / `Always hide` for relations; an empty inverse must not invent placeholder rows or files. Feasibility: **clear** (inherit fail-closed scan: `parseRelationValues(undefined)` → `[]`). Files: display consumers of items 2–3 only (no new persistence). Effort: **S**. Depends on: items 2–3. Citation: [notion.com/help/relations-and-rollups](https://www.notion.com/help/relations-and-rollups) (Display options for relations); Anytype empty → `null` at `anytype-ts/src/ts/component/block/featured.tsx:192-194`.

5. **Bounded inverse render window** — Notion’s API caps a relation value at 25 references with `has_more`; a Report with hundreds of Expenses must not dump unbounded chips on mobile. Feasibility: **clear** (display policy only; computation stays O(edges)). Files: whichever surface renders inverse chips (rollup `list` cell and/or item 6). Effort: **S**. Depends on: items 2–3. Citation: [developers.notion.com/reference/page-property-values#relation](https://developers.notion.com/reference/page-property-values#relation) (`has_more`).

6. **Record-page inbound section** — Notion’s two-way benefit is that the *page* shows related records; Anytype features `backlinks` on the object header. The fork’s record panel only renders stored relation columns (`RecordDetailPanel.ts:320-323`), so a Report with no stored back-property will not list Expenses unless a rollup column exists (item 2) or this section is added. Feasibility: **likely** (renderer reuse is proven) but **out of the spec file budget** until the operator expands SC-005. Files: `src/views/RecordDetailPanel.ts` (additive trailing section; do not change `renderRelationValue`’s signature). Effort: **S**. Depends on: items 1, 3, and an explicit budget waiver. Citation: `src/views/RecordDetailPanel.ts:320-323`; Anytype featured backlinks `anytype-ts/src/ts/lib/util/object.ts:983` (`recommendedFeaturedRelations: [ 'type', 'tag', 'backlinks' ]`).

7. **Refresh scoping without a second walker** — Notion stays consistent by dual-writing the mirror; the fork must recompute inbound from the in-memory cache and invalidate when source paths or `targetDatabaseId` change. Feasibility: **clear** if inverse is a pure per-refresh function called from `buildRelationRollups` (same cadence as today’s rollups). Files: no new plumbing if item 2 is inside `RelationRollup.ts`; reuse `targetPaths` / `relationTargetPaths` already stored at `src/views/EmbeddedDatabaseRenderer.ts:3210-3221` and `planRelationTargetChange` at `src/data/RelationTargetChange.ts:23-49`. Effort: **S**. Depends on: items 1–2. Citation: `src/data/RelationRollup.ts:20-21,36` (early-return trap: inverse **cannot** be a callee of `buildRelationRollups` when the viewed DB has zero rollup columns).

8. **Self-relation reverse index** — Notion’s only derived-like case: a self-relation with one property works both ways without a second field. Feasibility: **clear** (falls out of item 1; reverse index, no recursive expander). Files: none beyond item 1. Effort: **S**. Depends on: item 1. Citation: [notion.com/help/relations-and-rollups](https://www.notion.com/help/relations-and-rollups) (Relate a database to itself); fork dedupe `seenPaths` at `src/data/RelationRollup.ts:69-75`.

9. **Table-cell inbound badge (“↩ N”)** — Notion does **not** show inbound counts on table cells unless two-way is on (then it is a normal dual column); AppFlowy grid cells show only forward chips. Feasibility: **likely**, low value. Files: `src/views/CellRenderer.ts` (and compact siblings). Effort: **S**. Depends on: item 1. **Defer** to keep SC-005. Citation: AppFlowy `DesktopGridRelationCellSkin` at `appflowy/frontend/rust-lib/../appflowy_flutter/lib/plugins/database/widgets/cell/desktop_grid/desktop_grid_relation_cell.dart:15-110` (forward chips only).

10. **Stored two-way write-back (`syncWrites` ON / Notion `dual_property`)** — Exact Notion storage model: second property on the target and edits sync both ways. Feasibility: **blocked** this phase (REQ-006; spec out of scope). Files: would touch write paths in `src/data/DataSource.ts` (`writeQueues` at `:89`, `enqueueWrite` at `:99-120`, `mutateFrontmatter` at `:288-296`) plus the target note. Effort: **L** later. Depends on: an explicit later phase and a conflict policy that does not exist. Citation: “With two-way relations, the edits work both ways!” at [notion.com/help/relations-and-rollups](https://www.notion.com/help/relations-and-rollups); AppFlowy visible layer stores only forward `row_ids` (`appflowy/frontend/rust-lib/flowy-database2/src/entities/type_option_entities/relation_entities.rs:44-48` has `database_id` only — no invert flag).

## Recommended build (locked design)
**Module:** `src/data/RelationInverse.ts` — EuroFormat-style isolated file: durable-WHY header, no plugin hooks, no writes, no class. Mirror `src/data/EuroFormat.ts:1-10` (self-contained, rebasable). Declare `export const SYNC_WRITES_DEFAULT = false` on this module (zero `types.ts` touch).

**Exports (locked):**
- `RelationInverseContext { app, databases, getRecordsForDatabase }` — same `databases[]` + cache closure as `RelationRollupContext` (`src/data/RelationRollup.ts:10-16`), but **no** `sourceDatabase` / `sourceRecords` (fan-in is all DBs).
- `RelationInverseEdge { sourceDatabase, relationColumn, sourceRecord }`.
- `RelationInverseResult { inboundByPath: Map<string, RelationInverseEdge[]>, sourcePaths: Set<string> }`.
- `buildRelationInverse(context): RelationInverseResult`.

**Algorithm (same scan as `buildRelationRollups`, inverted — REQ-003 / NFR-P01):**
1. Fan-in index: for every DB in `context.databases`, collect columns where `column.type === "relation" && column.relationConfig?.targetDatabaseId` (identical filter: `src/data/RelationRollup.ts:28-32`), grouped by that id.
2. For target DB `T`, for each source `(db, column)` with `targetDatabaseId === T.id`, iterate `getRecordsForDatabase(db)` (in-memory only: `src/data/DataSource.ts:229-232`).
3. Per source record: `parseRelationValues(record.frontmatter[column.key])` (`src/data/RelationLinks.ts:23-26`) → `app.metadataCache.getFirstLinkpathDest(link.target, record.file.path)` (`src/data/RelationRollup.ts:71`) → skip `null` (dangling) → per-record `seenPaths` dedupe (`:69-75`) → membership against `T`’s `recordsByPath` (`:50-56,73-74`) → append the edge to `inboundByPath.get(resolved.path)`.
4. Do **not** call `buildRelationRollups` from the inverse (or vice versa as the sole entry): it early-returns when the *source* DB has no rollup columns (`src/data/RelationRollup.ts:36`). Inverse is its own entry with the same resolver/dedupe/membership shape. One pass over fan-in edges; no second vault walk; no `vault.*write*` / `processFrontMatter`.

**Call sites (spec-locked, 2 of 3 hunks — EuroFormat budget):**
1. **`src/data/RelationRollup.ts`** — inside `buildRelationRollups`, when `relationColumns.get(config.relationField)` is missing, resolve `relationField` as an inverse: a relation column on another database in `context.databases` whose `relationConfig.targetDatabaseId === context.sourceDatabase.id`. Feed those inbound records to existing `aggregateRollup` (`:92-129`). Do not add aggregation kinds. Do not change `RollupConfig` shape (`src/data/types.ts:39-45`); extend the *resolution rule* only. This is what makes a Report `count`/`list` of Expenses work without a stored back-property (SC-002/SC-003) while leaving `DatabaseView.ts:3388-3401` and `EmbeddedDatabaseRenderer.ts:3198-3208` unchanged — they already assemble the context and call `buildRelationRollups` when the viewed DB has rollup columns.
2. **`src/data/RelationLinks.ts`** — one exported helper (edge → `ParsedRelationLink` + label via `getRelationDisplayLabel`) so inverse chips stay on the existing contract. Rendering stays `renderRelationValue` (`src/views/RelationValueRenderer.ts:7-37`): empty → `false`, chips with `markNoteHoverLink` + `openLinkText`. Inverse click must pass the **source** record’s path as resolution context, not the Report’s.

**Third hunk:** leave unused unless the operator approves item 6 (`RecordDetailPanel.ts`). `SYNC_WRITES_DEFAULT` lives on the new module, so no type/config file.

**Write invariant:** relation edits keep flowing through `DataSource.enqueueWrite` keyed by the edited path (`src/data/DataSource.ts:89,99-120`; `mutateFrontmatter` at `:288-296`). `PropertyService.processFrontmatter` already delegates to that queue (`src/data/PropertyService.ts:181-188`; wired from `DatabaseView.ts:498-504`). `RelationInverse.ts` never joins the queue.

## Edge cases & mobile/iCloud safety
**Must handle (spec §8, fork-confirmed):**
- Empty relation → `[]` inbound, no placeholder rows, no file touch (`parseRelationValues` at `src/data/RelationLinks.ts:23-26`; Anytype `featured.tsx:192-194`; Notion Hide when empty).
- Cardinality 1 (Expense.Month → one Report) → inverse is still a **list** of sources (Notion “1 page” forward limit does not make the back-set a singleton).
- Many-to-one → union of matching sources; `count`/`list` consume that set (SC-003).
- Dangling wikilink → `getFirstLinkpathDest` returns null, `continue`; never create/repair the target (`src/data/RelationRollup.ts:71-72`).
- Cross-database miss → resolved path not in target `recordsByPath` is skipped (`:73-74`). Honor `RelationConfig.targetDatabaseId` (`src/data/types.ts:34-37`); stricter than Anytype’s space-wide `backlinks` (`anytype-ts/src/ts/lib/util/object.ts:494`).
- Alias/`#` subpath → strip before resolve (`src/data/RelationLinks.ts:15-19`).
- Duplicate wikilinks on one record → `seenPaths` (`src/data/RelationRollup.ts:69-75`).
- Circular / self-relation → reverse index of stored edges, not a recursive expander; a self-link appears once. Matches Notion’s single-property self-relation.
- Multi-DB fan-in → union across every database whose relation columns target `T` (structural delta vs `buildRelationRollups`, which only iterates one `sourceDatabase`).
- Concurrent clicks on different Expenses → two `writeQueues` keys; Report is not a participant (`src/data/DataSource.ts:89,99-120`).
- View refresh while a source write is queued → read `recordCache` / `getRecordsForDatabase` (`:229-232,239-244`); never flush repair writes to the target. Plugin writes credited via `ownedPathUntil` (`:81-84,246-249`).
- Scan API mismatch → call site fail-closed to empty inbound; never invent a second scanner.
- Large inbound sets → bound the **render** (first N + “+M more”, N=25 unless operator picks otherwise); computation stays one O(edges) pass.
- `syncWrites` true → out of scope; module contains no write branch; compiled default is OFF.

**Why this is safe on mobile + iCloud:** the inverse is display-only derived data, the same class as rollups (“never written to frontmatter”, `src/data/types.ts:69`). Default path dirties one markdown file per relation click (SC-001/REQ-005); iCloud sees one note. No extra `writeQueues` work on view open (NFR-P02). APIs used are cross-platform Obsidian (`metadataCache.getFirstLinkpathDest`, `workspace.openLinkText`, `setIcon`, DOM `createDiv`/`createEl`); no `electron` / `node:` / `fs`. MIT-forkable; no telemetry. Bounded chip windows are the only mobile-specific constraint (render cost, not writes). Anytype ships the same storage theorem at scale: one stored edge, `backlinks` read-only (`anytype-ts/src/ts/lib/util/object.ts:494`; `featured.tsx:185-208`). AppFlowy’s visible layer does not compute an inverse at all (no `invert` in `flowy-database2`); its UI still validates forward chips + navigate-to-related-row, which the fork already has.

## Open questions / operator decisions
1. **SC-002 surface: rollup-only vs record-page section.** Default: **rollup-only** this phase (items 1–2 inside `RelationRollup.ts` + `RelationLinks.ts`) so SC-005 stays two hunks. Approve item 6 (`RecordDetailPanel.ts` trailing “Backlinks” section, hide when empty) only if Reports without a rollup column must still list Expenses on the record panel.
2. **`RollupConfig.relationField` inverse resolution vs new schema field.** Default: **resolution rule only** (when the key is not a local relation, treat it as the key of a relation column on another DB targeting this one). No `types.ts` change. A new config field would be a schema migration and a fourth-file risk.
3. **Chip window size N.** Default: **25**, matching Notion `has_more`.
4. **Section / helper label.** Default: **“Backlinks”** (Obsidian-native; distinguish from core backlinks via existing relation-chip styling, not new copy). Finance already names both directions conceptually; do not persist a reverse property to match the name.
5. **Sort order of inbound chips.** Default: **sort at display time** by `getRelationDisplayLabel` (`src/data/RelationLinks.ts:29-31`); keep data-layer order as scan order (deterministic via `databases[]` + cached record list, same as rollups).
6. **Verification harness.** Research confirmed `vitest.config.ts` includes `src/**/*.test.ts` and `setupFiles: ["src/__tests__/setup.ts"]`, but `src/` has **zero** `*.test.ts` files, `src/__tests__/` is missing, and `package.json` scripts are only `dev`/`build`/`lint`/`lint:all` (lint ignores `src/__tests__/**`). Default: **bootstrap this phase** (`src/__tests__/setup.ts`, `"test": "vitest run"`, `src/data/RelationInverse.test.ts`) so SC-001 can spy `enqueueWrite` and SC-002/003 can fixture forward-only wikilinks. This is verification logistics, not a Notion gap.
7. **Do not enable `syncWrites`.** Default: **OFF forever in this phase**; no ON path, no conflict policy. AppFlowy’s likely mirror-field (non-vendored `collab-database`) and Notion `dual_property` are the cost this packet exists to avoid.
