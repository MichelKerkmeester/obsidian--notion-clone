# Handover: card-title-production (worktrees/226-card-title-production)

## What this leg was

Queue item `058-title-production`: a fresh operator report (2026-09-07 ~22:05, 0.0.31 iOS) showed
board cards titled by their raw file names (`3537.32`, `4736.32`) above a path line
`Finance/Reports/03 • Mar '25`. Verbatim: *"we could change number format (eur, dollar etc) for
board card names like attaches AND select which field is used as the file name so that instead of
the number we would see the month for example. But was never implemented it looks like."*

The prior packet, `specs/005-component-surface-system/058-card-title-and-title-formats`, landed at
`74268259` claiming AC-001 through AC-007 Met, but its own log named D1: the screenshot evidence
was hand-written fixture HTML, never the production `BoardRenderer`. This leg's job was to
reproduce RED on the real renderer, then close whatever it found.

## What was RED, and what was not

1. **The per-view title-field picker already existed and was already reachable on phone**
   (`ViewConfig.titleField`, `board-card-properties-panel.ts`'s Title row jump affordance to
   `view-config-panel-renderer.ts`'s general-section dropdown). Confirmed by reading source and by
   the existing `panel-view-config` live-harness scenario. **Not red.**
2. **A currency-typed `titleField`'s format routing was already correct in production** — the
   `board-title-currency-column` scenario I added to `tools/live/render-assertion-harness.ts` mounts
   the real `BoardRenderer` with a currency column as `titleField` and passed on the FIRST run,
   before any code change. **D1 was an evidence gap, not a behavior gap** — the earlier fixture
   captures happened to be right.
3. **The operator's actual defect — a numeric file name with no format — was real and unbuilt.**
   `resolveTitleFieldDisplay`'s file-title branch (`titleField` unset/`file.name`) had no format
   option at all; `grep -rn "titleFormat" src` returned nothing. **Confirmed red** via new unit
   tests (`title-field-display.test.ts`) and panel tests (`view-config-panel-renderer.test.ts`),
   both failing against the unmodified tree.
4. **A second, deeper red, found only by driving production**: once the file-name `titleFormat`
   field existed and its unit tests passed, the board card still showed the raw file name.
   `board-renderer.ts`'s `getReferenceRowTitle` special-cased `title.isFileTitle` to read
   `row.file.basename` directly instead of `title.text` — a shortcut that predates this leg,
   harmless while the two were always identical, silently wrong the moment `titleFormat` made them
   diverge. No unit test could have caught this (they call the resolver directly); no fixture-based
   screenshot could have caught it either (a hand-written fixture never calls
   `getReferenceRowTitle`). Confirmed red on the real `board-title-format-numeric-filename` live
   scenario, green after the fix.

## What shipped

- `src/data/types.ts`: `TitleFileFormat` type, `ViewConfig.titleFormat`.
- `src/data/title-field-display.ts`: `formatFileTitleText` routes the file-title branch through
  `titleFormat` (text/number/currency-eur/currency-usd/currency-gbp/date); local USD/GBP
  `Intl.NumberFormat` instances (kept out of `euro-format.ts`, which stays euro-only by design).
- `src/views/view-config-panel-renderer.ts`: a "Title format" row beside the existing Title field
  row, visible only while the title reads the file name.
- `src/views/board-renderer.ts`: `getReferenceRowTitle` now reads `title.text` unconditionally
  (the bug fix — see #4 above).
- `src/i18n.ts`: new `viewConfig.titleFormat*`/`undo.titleFormatConfig` keys, all three locales.
- `tools/live/render-assertion-harness.ts` + `render-assertion-bundle.mjs`: two new scenarios
  (`board-title-currency-column`, `board-title-format-numeric-filename`) mounting the real
  `BoardRenderer`, plus `boardCardTitleFormatAssertion`.
- `tools/screenshots/constructed-scenarios.mjs` + `scenarios/core.mjs`: two new constructed
  (real-renderer-driven) captures, `constructed-board-title-currency` and
  `constructed-board-title-format-filename`, both themes/devices, opened and read; the original
  `board-card-title-currency` fixture cross-linked via `fixtureOf`.
- Spec docs: `058`'s `goal.md` (D7/D8), `spec.md` (REQ-005, scope, files-to-change), `plan.md`
  (FIX ADDENDUM corrected — `board-renderer.ts` was NOT unchanged, contrary to what it said),
  `tasks.md` (Phase 4, T012-T017), `acceptance-criteria.md` (AC-009, AC-010), `decision-record.md`
  (ADR-005, ADR-006). `../roadmap.md` §4 gained row 67 for this operator report.
- `tools/naming/failing-values-baseline.json`: raised 147 -> 148, one justified row (AC-009's own
  goal.md criterion — the currency-column claim was never actually broken, so it has no failing
  value to record; the file-name titleFormat fix DOES carry one and needed no baseline change).
- `tools/lane/css-lane.json`: styles.css itself was never touched (hash unchanged, still held by
  `068-rename-to-obnotion` — no acquire needed); appended a `release` entry naming the 12 captures
  this leg's code changes moved (all opened and read), and restoring one unrelated 2-byte
  encoder-jitter capture (`board-mobile-desktop-dark.png`, a fixture untouched by this leg's code)
  to its committed bytes rather than leaving it drifting.

## Verification (all read from `$?`, not assumed)

- `npx tsc --noEmit`: exit 0
- `npx vitest run`: exit 0, **1655/1655** on this worktree's tree (origin/main at `5e7f1426` plus
  this leg's own new/edited tests; not compared against 058's original 1520 baseline, since other
  legs have landed on `origin/main` since then)
- `npm run build`: exit 0
- `node tools/live/render-assertions.mjs`: exit 0, including the two new scenarios above
- `node tools/live/sheet-grammar.mjs`: exit 0
- `npm run gate` (foreground, `stdin </dev/null`, never backgrounded by the shell — the tool's own
  job supervision is separate from that): **exit 0, 26 green, 0 red**. Two transient failures on
  the first run (`sheet-rebuild`'s known-flaky real-pointer gesture test, and `evidence` going
  stale as a direct consequence) both cleared on an immediate rerun with zero code changes —
  confirmed flaky, not a regression, by rerunning `sheet-rebuild.mjs` standalone and watching it
  pass.
- `npm run replay`: exit 0, all 28 hold, 0 reversed
- `node tools/naming/scan-comments.mjs`: exit 0 (0 artifact-id violations — this required rewriting
  several comments that had cited "058"/"ADR-003"/"AC-001"/"D1" per the comment-hygiene hard block)
- `node tools/naming/scan-failing-values.mjs`: exit 0, baseline raised by one justified row (above)
- `npm run screenshots` + `npm run screenshots:verify`: exit 0, 616/616 (was 608; +8 for the two
  new constructed scenarios, both themes/devices)
- `node tools/lane/check-lane.mjs`: exit 0
- Spec validation: `NODE_PRESERVE_SYMLINKS=1 bash "$(realpath .opencode)/skills/system-spec-kit/runtime/cli/spec/validate.sh" specs/005-component-surface-system/058-card-title-and-title-formats --strict` — first `RESULT: PASSED`, 0 errors, 1 pre-existing warning (a single-token trigger phrase in `decision-record.md`'s frontmatter, present before this leg, not touched)
- Metadata regenerated: `node "$(realpath .opencode)/skills/system-spec-kit/runtime/cli/dist/graph/backfill-graph-metadata.js" specs/005-component-surface-system/058-card-title-and-title-formats`

## What a person confirmed by opening the image

- `constructed-board-title-currency-{mobile,desktop}-{dark,light}.png`: card titles read
  `€ 0,5` / `€ 37,5` / `€ 185,5` / `€ 222,5` — the schema's currency column, formatted, never the
  raw stored number.
- `constructed-board-title-format-filename-{mobile,desktop}-{dark,light}.png`: card titles read
  `€ 3.537,32` / `€ 9.532,82` / `€ 4.736,42` / etc. — numeric file names (mirroring the operator's
  own report almost digit-for-digit) formatted through the new file-name `titleFormat`.
- `constructed-board-card-properties-{mobile,desktop}-{dark,light}.png`: the settings sheet now
  shows "Title field" -> "Use file name" immediately followed by "Title format" -> "Plain text",
  in both the phone sheet's `.obnotion-panel-row` grammar and the desktop popover's two-column row.

## What is NOT done here (by design)

- **Not pushed.** HEAD is uncommitted local work in this worktree
  (`worktrees/226-card-title-production`, branch tracking `origin/main`). A fresh verifier rebases
  onto whatever `origin/main` has moved to (other legs are in flight) and lands it.
- AC-008 (the operator's own device confirmation) is unchanged and stays open — no agent may tick
  it, per the packet's own D3.
- The `068-rename-to-obnotion` css-lane holder is unchanged — this leg made no stylesheet edit and
  did not need to take the lane over.

## For the verifier

1. Rebase this branch onto current `origin/main` (other legs are landing concurrently; expect
   `src/views/board-renderer.ts` to have moved under `getReferenceRowTitle` or nearby — re-verify
   the file-title branch reads `title.text` unconditionally after the rebase, not the reverted
   `title.isFileTitle ? row.file.basename : title.text` shortcut).
2. Re-run the full verification list above from the rebased tree before landing — do not trust
   this session's numbers past the rebase.
3. `npm run screenshots` after the rebase if any of `board-renderer.ts`, `title-field-display.ts`,
   `view-config-panel-renderer.ts`, or `styles.css` moved under the rebase; re-open and re-read the
   changed captures (this leg's own `tools/lane/css-lane.json` release entry, or a fresh one, must
   name whatever the rebase itself moves — this leg's entry only accounts for what THIS leg moved).
4. AC-008 stays for the operator; do not close the packet on this leg's evidence alone.
