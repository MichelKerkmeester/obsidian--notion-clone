# live-host-model — working notes (scratch, not a spec doc)

Started: 2026-09-07, worktree `.worktrees/221-live-host-model`, HEAD 6f4d026c (fresh off origin/main, symlinks pre-placed by caller: `.opencode`, `node_modules`, `specs/context`).

Task (from `glm/queue/live-host-model.prompt`):
1. Give `tools/live/` the same host stylesheet model that `tools/storybook/verify-placement.mjs`'s `HOST_BARE_CONTROLS` block provides — one shared module both import (no `--input-height`, no host `button` rule: `white-space: nowrap; justify-content: center; height: var(--input-height)`).
2. Run every live lane (sheet-grammar, render-assertions, touch-targets, sheet-rebuild, sheet-teardown) and reconcile every row that moves:
   - row goes red under host model = real defect on device → record as task row in owning packet with numbers (no product-code fix beyond one-line CSS the lane proves)
   - row's threshold was written against the host-less model → re-derive threshold, record reason
   - Prove the 054 ink row now trips when the placement-option wrap rule is deleted.
3. `tools/live/sheet-rebuild.json` filter-sheet row: re-measures 527/627 vs main's 836/836 on byte-identical inputs — find cause, make deterministic, record finding.
4. `tools/lane/check-lane.mjs`'s `git show` call: no `maxBuffer` override, `screenshots/manifest.json` now >1MB (Node default) — add explicit maxBuffer (e.g. 64MB), add a test reading a >1MB blob.
5. Comment hygiene hard block (no ephemeral artifact labels in code comments).
6. Verify: `npx tsc --noEmit`, `npx vitest run`, every live lane, `npm run gate` once in foreground (stdin /dev/null, timeout 600000, not backgrounded) — 26 green; scan-comments/scan-failing-values exit 0; validate owning packet `--strict` via realpath'd orchestrator (first RESULT PASSED); scoped graph metadata backfill.
7. Commit (no push) with the given trailers. Report HEAD SHA, every row moved (before/after + disposition), the sheet-rebuild finding, exit statuses.

Constraints: do ALL work myself, never spawn a sub-agent. Class prefix is `obnotion-` (068 rename landed at 6f4d026c) — use landed names everywhere. Do not push; a fresh verifier rebases and lands. A 0.0.31 release commit may land on main while I work — not my concern, I stay on this worktree branch.

Owner packet: TBD — must read both `specs/.../009-live-verification` and `.../054-...` and pick by goal (live lanes' fidelity), state why, in the final handover entry.

## Progress log
- [x] Read 009 vs 054 packets, pick owner → 009-live-verification (its whole charter is live-lane
      fidelity/harness truth; 054 is a consumer whose ink row depends on the fix, not the owner of
      lane infrastructure)
- [x] Build shared host-model module → tools/screenshots/host-bare-controls.css (CSS file,
      readFileSync'd by both sides, mirrors the tools/screenshots/host-checkbox.css precedent)
- [x] Wired into tools/storybook/verify-placement.mjs (inline HOST_BARE_CONTROLS const replaced by
      readFileSync of the shared file) and all 5 named tools/live/ lanes (sheet-grammar,
      render-assertions [4 sites], touch-targets, sheet-rebuild [index.html + parity.html],
      sheet-teardown)
- [x] sheet-grammar.mjs's placement-ink negative control now reads the host's live cascade off a
      bare <button> probe instead of hand-typed 44px/nowrap/center values — ties it to the shared
      file so it can't drift
- [x] Investigated CSSOM rule-deletion for a literal "delete the wrap rule" control — Chromium
      blocks .cssRules on a file:// <link> stylesheet (reproduced with a minimal repro);
      addStyleTag-inserted inline styles ARE readable. Used the live-probe override instead of
      restructuring the whole page's stylesheet loading to inline <style> (too invasive here).
- [x] sheet-rebuild.mjs nondeterminism (item 3) root-caused and fixed: `openSettled`'s
      waitForFunction resolved "settled" off two consecutive equal raf samples with no
      minimum-elapsed-time floor, so the FIRST sheet a fresh page opens can read its
      pre-transition top on two frames 11ms apart (cold-start race in when the app's own
      .is-visible-adding rAF fires relative to the poll). Added ENTRANCE_SETTLE_FLOOR_MS=260
      (200ms token + margin). Verified 4/4 clean runs before the fix reproduced sort=836/836
      (WRONG, pre-transition top — matches "main recorded 836/836") + filter=541/641 (already
      correct, close to the 527/627 the brief measured); 4/4 runs after the fix give
      sort=674/655/674, filter=541/641/641 — stable, matching real animated resting positions.
- [x] Fixed check-lane.mjs's git show maxBuffer (64MB) + added a real disposable-repo test
      (tools/lane/check-lane.test.mjs) committing a >1MB manifest.json; verified the test fails
      without the fix (ENOBUFS -> null) and passes with it.
- [x] Recovered a git-stash mishap (stash pop conflicted on sheet-rebuild.mjs since I'd since
      edited it in place for item 3 without popping first) — manually re-merged the host-model hunk
      into sheet-rebuild.mjs, restored the other 4 files via `git checkout stash@{0} --`, verified
      every file still `node --check`s clean and the host-model grep counts match, then dropped the
      stash.
- [x] Ran every live lane with host model on, reconciled every row that moved: sheet-grammar's ink
      row re-derived (19px -> 30px, real host `display:inline-flex` on a bare button changes the
      overflow threshold); host-bare-controls.css's padding hardcoded to 4px/12px (var(--size-4-1/3)
      were unresolved in lanes without theme.css, silently giving buttons MORE room than a device
      has); found + deferred a real .obnotion-panel-button padding defect (~10px overflow,
      sort-panel, both engines) as 009's T26, with a matching tools/gate.mjs expectFail;
      touch-targets' two ratchets tightened (185->171, 807->785, zero regressions, 3 reproducing
      runs each); render-assertions and sheet-teardown unaffected.
- [x] Proved the 054 ink row trips when the placement-option wrap rule is literally deleted from
      styles.css (commented out, reran, "3/3 buttons paint ink outside their own box at 30px (worst
      81.0px)", restored clean — git diff empty after restore).
- [x] Comment hygiene: scan-comments exit 0, 0 artifact-id violations.
- [x] Full verification: tsc 0; vitest 153 files/1642 tests; all 5 lanes run individually
      (sheet-grammar exits 1 for the declared T26 defect, others exit 0); npm run gate </dev/null>
      foreground, exit 0, "25 green, 1 red for a declared reason"; scan-failing-values exit 0;
      009-live-verification validated --strict RESULT: PASSED (after backfill-graph-metadata.js);
      005-component-surface-system (parent) also re-validated --strict RESULT: PASSED after the
      handover.md edit (had to trim frontmatter to satisfy FRONTMATTER_MEMORY_BLOCK's compactness
      and 2048-byte caps — first attempt failed on that, fixed and reconfirmed).
- [x] Commit (pending, next action)
- [x] Wrote the packet handover entry in specs/005-component-surface-system/handover.md
- [ ] Final report to caller
