# live-host-model — working notes (scratch, not a spec doc)

Started: 2026-09-07, worktree `.worktrees/221-live-host-model`, HEAD 6f4d026c (fresh off origin/main, symlinks pre-placed by caller: `.opencode`, `node_modules`, `specs/context`).

Task (from `glm/queue/live-host-model.prompt`):
1. Give `tools/live/` the same host stylesheet model that `tools/storybook/verify-placement.mjs`'s `HOST_BARE_CONTROLS` block provides — one shared module both import (no `--input-height`, no host `button` rule: `white-space: nowrap; justify-content: center; height: var(--input-height)`).
2. Run every live lane (sheet-grammar, render-assertions, touch-targets, sheet-rebuild, sheet-teardown) and reconcile every row that moves:
   - row goes red under host model = real defect on device → record as task row in owning packet with numbers (no product-code fix beyond one-line CSS the lane proves)
   - row's threshold was written against the host-less model → re-derive threshold, record reason
   - Prove the 054 ink row now trips when the placement-option wrap rule is deleted.
3. `tools/live/sheet-rebuild.json` filter-sheet row: re-measures 527/627 vs main's 836/836 on byte-identical inputs — find cause, make deterministic, record finding.
4. `tools/lane/check-lane.mjs`'s `git show` call: no `maxBuffer` override, `screenshots/manifest.json` now >1MB (Node default) — add explicit maxBuffer (e.g. 64MB), add a test reading a >1MB blob.
5. Comment hygiene hard block (no ephemeral artifact labels in code comments).
6. Verify: `npx tsc --noEmit`, `npx vitest run`, every live lane, `npm run gate` once in foreground (stdin /dev/null, timeout 600000, not backgrounded) — 26 green; scan-comments/scan-failing-values exit 0; validate owning packet `--strict` via realpath'd orchestrator (first RESULT PASSED); scoped graph metadata backfill.
7. Commit (no push) with the given trailers. Report HEAD SHA, every row moved (before/after + disposition), the sheet-rebuild finding, exit statuses.

Constraints: do ALL work myself, never spawn a sub-agent. Class prefix is `obnotion-` (068 rename landed at 6f4d026c) — use landed names everywhere. Do not push; a fresh verifier rebases and lands. A 0.0.31 release commit may land on main while I work — not my concern, I stay on this worktree branch.

Owner packet: TBD — must read both `specs/.../009-live-verification` and `.../054-...` and pick by goal (live lanes' fidelity), state why, in the final handover entry.

## Progress log
- [x] Read 009 vs 054 packets, pick owner → 009-live-verification (its whole charter is live-lane
      fidelity/harness truth; 054 is a consumer whose ink row depends on the fix, not the owner of
      lane infrastructure)
- [x] Build shared host-model module → tools/screenshots/host-bare-controls.css (CSS file,
      readFileSync'd by both sides, mirrors the tools/screenshots/host-checkbox.css precedent)
- [x] Wired into tools/storybook/verify-placement.mjs (inline HOST_BARE_CONTROLS const replaced by
      readFileSync of the shared file) and all 5 named tools/live/ lanes (sheet-grammar,
      render-assertions [4 sites], touch-targets, sheet-rebuild [index.html + parity.html],
      sheet-teardown)
- [x] sheet-grammar.mjs's placement-ink negative control now reads the host's live cascade off a
      bare <button> probe instead of hand-typed 44px/nowrap/center values — ties it to the shared
      file so it can't drift
- [x] Investigated CSSOM rule-deletion for a literal "delete the wrap rule" control — Chromium
      blocks .cssRules on a file:// <link> stylesheet (reproduced with a minimal repro);
      addStyleTag-inserted inline styles ARE readable. Used the live-probe override instead of
      restructuring the whole page's stylesheet loading to inline <style> (too invasive here).
- [x] sheet-rebuild.mjs nondeterminism (item 3) root-caused and fixed: `openSettled`'s
      waitForFunction resolved "settled" off two consecutive equal raf samples with no
      minimum-elapsed-time floor, so the FIRST sheet a fresh page opens can read its
      pre-transition top on two frames 11ms apart (cold-start race in when the app's own
      .is-visible-adding rAF fires relative to the poll). Added ENTRANCE_SETTLE_FLOOR_MS=260
      (200ms token + margin). Verified 4/4 clean runs before the fix reproduced sort=836/836
      (WRONG, pre-transition top — matches "main recorded 836/836") + filter=541/641 (already
      correct, close to the 527/627 the brief measured); 4/4 runs after the fix give
      sort=674/655/674, filter=541/641/641 — stable, matching real animated resting positions.
- [x] Fixed check-lane.mjs's git show maxBuffer (64MB) + added a real disposable-repo test
      (tools/lane/check-lane.test.mjs) committing a >1MB manifest.json; verified the test fails
      without the fix (ENOBUFS -> null) and passes with it.
- [x] Recovered a git-stash mishap (stash pop conflicted on sheet-rebuild.mjs since I'd since
      edited it in place for item 3 without popping first) — manually re-merged the host-model hunk
      into sheet-rebuild.mjs, restored the other 4 files via `git checkout stash@{0} --`, verified
      every file still `node --check`s clean and the host-model grep counts match, then dropped the
      stash.
- [x] Ran every live lane with host model on, reconciled every row that moved: sheet-grammar's ink
      row re-derived (19px -> 30px, real host `display:inline-flex` on a bare button changes the
      overflow threshold); host-bare-controls.css's padding hardcoded to 4px/12px (var(--size-4-1/3)
      were unresolved in lanes without theme.css, silently giving buttons MORE room than a device
      has); found + deferred a real .obnotion-panel-button padding defect (~10px overflow,
      sort-panel, both engines) as 009's T26, with a matching tools/gate.mjs expectFail;
      touch-targets' two ratchets tightened (185->171, 807->785, zero regressions, 3 reproducing
      runs each); render-assertions and sheet-teardown unaffected.
- [x] Proved the 054 ink row trips when the placement-option wrap rule is literally deleted from
      styles.css (commented out, reran, "3/3 buttons paint ink outside their own box at 30px (worst
      81.0px)", restored clean — git diff empty after restore).
- [x] Comment hygiene: scan-comments exit 0, 0 artifact-id violations.
- [x] Full verification: tsc 0; vitest 153 files/1642 tests; all 5 lanes run individually
      (sheet-grammar exits 1 for the declared T26 defect, others exit 0); npm run gate </dev/null>
      foreground, exit 0, "25 green, 1 red for a declared reason"; scan-failing-values exit 0;
      009-live-verification validated --strict RESULT: PASSED (after backfill-graph-metadata.js);
      005-component-surface-system (parent) also re-validated --strict RESULT: PASSED after the
      handover.md edit (had to trim frontmatter to satisfy FRONTMATTER_MEMORY_BLOCK's compactness
      and 2048-byte caps — first attempt failed on that, fixed and reconfirmed).
- [x] Commit (pending, next action)
- [x] Wrote the packet handover entry in specs/005-component-surface-system/handover.md
- [ ] Final report to caller

## Verifier log (2026-09-07, dispatched landing verifier)
### Step 1 — repo state
- `git status --short | wc -l` → 0 (clean, .handover.md tracked+clean too).
- `git log --oneline origin/main..HEAD` → exactly 1 leg commit:
  77397c4b "feat(tools-live): give tools/live/ the host stylesheet model verify-placement.mjs already carried"
  (25 files: host-bare-controls.css new; 5 lanes + verify-placement wired; baselines; 009 tasks/graph-metadata; 005 handover; gate expectFail; check-lane + test).
### Step 2 — claims (a)(b)(c)
- (a) CONFIRMED. `host-bare-controls.css` referenced by: verify-placement.mjs:65 (readFileSync, old
  inline block gone — only comment/probe refs remain), sheet-grammar.mjs:2133 (<link>), 
  render-assertions.mjs ×4 (598 <link> + 723/792/829 sheet lists), touch-targets.mjs:194
  (readFileSync), sheet-rebuild.mjs ×2 (162 index.html <link>, 172 parity.html inline),
  sheet-teardown.mjs:94 (<link>). Shared file = 50 lines, carries the full host button rule
  (inline-flex/center/nowrap, height 30px, padding 4px/12px) + the reasons.
- (b) CONFIRMED. Wrapped the .obnotion-view-config-panel.obnotion-mobile-bottom-sheet
  .obnotion-new-placement-option rule (styles.css:23366) in /* VERIFIER-TEMP */; `node
  tools/live/sheet-grammar.mjs` exit 1 (expected: extra reds from rule-off + declared T26), printed
  verbatim: "settings sheet placement-button ink: 3/3 buttons paint ink outside their own box at
  30px (worst 81.0px)" — same worst-81.0px the leg recorded. `git checkout -- styles.css` → diff
  empty.
- (c) CONFIRMED. `git diff origin/main -- tools/live/touch-targets*baseline.json`: ONLY "under"
  changed numerically — fixture 185→171, constructed 807→785, both DOWN; other hunks are the added
  `hostStylesheetModelLowering` prose key + trailing commas. No raised number anywhere.
### Step 2 — claims (d)(e)(f)(g)
- (d) CONFIRMED. ENTRANCE_SETTLE_FLOOR_MS = 260 at sheet-rebuild.mjs:207, consumed at :747.
  Ran the lane 3× (exit 0, 0, 0); sort check identical all 3 runs ("settled at top 674, rebuilt to
  655; deepest 674"), filter check identical all 3 runs ("settled at top 541, rebuilt to 639;
  deepest 639"), every check pass:true. No 836/836 pre-transition top ever reappeared.
  Side effect: each run re-stamps tools/live/sheet-rebuild.json's measuredAt (1-line diff) —
  reconciled at step 6.
- (e) CONFIRMED. maxBuffer: 64*1024*1024 at check-lane.mjs:208 on the readManifestAtHead git show.
  Green: vitest tools/lane/check-lane.test.mjs → 27/27, exit 0. Mutation (maxBuffer line removed →
  node 1MB default): exactly "parses a committed manifest bigger than the default pipe buffer"
  fails (1 failed | 26 passed), exit 1. Restored (git checkout --): 27/27, exit 0.
- (f) CONFIRMED (mechanism + declaration; final print at step 7). CHECKS has 26 entries; expectFail
  on sheet-grammar names exactly T26's defect: ".obnotion-panel-button ... sort-panel's ~10px past
  its surface's right edge (Chrome and WebKit, as built/stacked/long-name)" — a declared reason,
  no threshold edited anywhere (expectFail only marks the lane, gate.mjs:164,189,208). 1 expectFail
  + lane red ⇒ prints "25 green, 1 red for a declared reason", exit 0 (gate.mjs:207-209). Note:
  "26 green, 0 red" (step 7) requires the expectFail DISCHARGED — it prints "unexpected pass" and
  fails the gate until the declaration is removed. T26 (009 tasks.md:194) is unticked/open; unless
  the rebase brings the padding fix, expect the honest result "25 green, 1 red for a declared
  reason", exit 0. Recorded, decision deferred to step 7 evidence.
- (g) DEFERRED BY DESIGN: vitest 1642 re-proven at step 5; 009+parent validate --strict re-proven
  at step 8 (STOP-if-stale rule lives there). Claim accepted from the leg's own recorded run until
  then.
### Step 3 — mutation tests for leg-added test files
- The leg added NO new test FILE: `git diff --diff-filter=A --name-only origin/main..HEAD` →
  .handover.md + tools/screenshots/host-bare-controls.css only. The sole test-bearing change is
  tools/lane/check-lane.test.mjs (+45: the "reading the committed manifest past node's default
  1MB pipe" describe block). Its named mutation — maxBuffer back to default — was exercised in
  (e): red (1 failed | 26 passed, exit 1, the >1MB test) → restore → green (27/27, exit 0).
  Step 3 therefore: satisfied by the (e) mutation, nothing further to mutate.
### Step 4 — fetch + rebase onto origin/main
- `git fetch origin` exit 0. New on main: 5e7f1426 (chore(release): cut 0.0.31) + c5b20ee4
  (docs: 005 operator state refresh) — 7 files, NO main.js / NO screenshots/manifest.json / NO
  styles.css. True overlap with the leg: ONLY the PARENT's specs/005.../graph-metadata.json +
  specs/005.../handover.md.
- `git stash`ed .handover.md (and discarded the measuredAt re-stamp) → rebase → 2 conflicts, both
  predicted. NOTE: verified `git show :2:file` == `origin/main:file` for BOTH (ours=main during
  rebase) before trusting --ours.
  - parent graph-metadata.json: 3 fingerprint lines conflicted → took MAIN's side (--ours);
    step 8's backfill re-derives (and AGAIN after step 11's handover edit — the leg's own
    precedent; otherwise GENERATED_METADATA_INTEGRITY goes stale).
  - handover.md: 4 hunks. Hunks 1-3 (description, continuity scalars+blockers, session_id) →
    MAIN's/223's side (fresher; the leg's 5th blocker duplicates 223's #1 "009 T26 ... expectFail").
    Hunk 4 (§1 insertion) → BOTH sections kept, 223's (~22:05) above the leg's (late evening),
    newest-first per the file's convention. completion_pct 85 + answered_questions' 0.0.31 line
    auto-merged from main. 0 markers left. Result: the leg's handover.md contribution = its
    section insert only (74 added lines); its continuity intent lives in the section until
    step 11's refresh.
- `git rebase --continue` exit 0. Leg re-hashed: 77397c4b → **e795fc55** ("feat(tools-live):
  give tools/live/ the host stylesheet model verify-placement.mjs already carried"), now 1 commit
  ahead of origin/main@c5b20ee4. Leg diff vs NEW main: 24 files, +451/−139 (was 25/+461/−148 —
  delta = the parent's graph-metadata.json + the superseded continuity hunks, both now main's
  side). .handover.md still added by the leg.
- css-lane sanity (not required — no conflict): pinned baselineHash 65c9009170d1 ==
  shasum(styles.css) — neither side touched styles.css. MATCH.
- Stash popped cleanly; working tree = .handover.md only.
### Step 5 — build + tsc + vitest
- `npm run build` exit 0 (run twice; first run's log additionally printed a stray "Command exited
  with code 1" line that the second's 4-line log did not — both invocations exited 0, second is
  the recorded one; watchChild parting line, not a failure). `main.js` rewritten at 23:03 and
  BYTE-IDENTICAL to the committed blob (git status: only .handover.md) — nothing to re-derive.
- `npx tsc --noEmit` → 0.
- `npx vitest run` → 0; **153 files / 1642 tests** — matches the leg's claimed 1642 exactly
  (claim (g) vitest component CONFIRMED; the validations re-proven at step 8).
### Step 6 — screenshots ×2 + artefact reconciliation
- pixel-delta.mjs present (scratchpad, 2321B). `npm run screenshots </dev/null` TWICE: exit 0, 0 —
  "done: 608 screenshots" both runs. Working tree after: ONLY .handover.md — zero moved PNGs,
  manifest/README byte-identical to committed.
- `node <scratchpad>/glm/pixel-delta.mjs` exit 0: "0 moved PNG(s) this run". No jitter, no real
  changes → nothing to checkout/patch, NO css-lane acquire/edit/release triplet (styles.css
  unchanged — pinned baselineHash still 65c9009170d1), no PNG/manifest reconciliation.
- Staleness: `node tools/live/evidence.mjs --check-all` exit 0 — all 15 tools/live/*.json "still
  describe this tree"; NOTHING stale → no lane re-derivation. `build-operator-checklist.mjs
  --check` exit 0 (179 rows / 62 phases, current) → no checklist rebuild.
### Step 7 — THE GATE
- `npm run gate </dev/null > $PWD/.gate-15063.log 2>&1` (foreground, stdin /dev/null), 23:19:51→
  23:22:22 = **2m31s**. `.gate-exit` = **0**. 26 lanes: 25 green + `sheet-grammar  red (expected)`.
  Printed: "gate: PASS — 25 green, 1 red for a declared reason" + the T26 reason, owner
  005-component-surface-system/009-live-verification. No lane unexpectedly red → nothing to
  re-derive, no second gate run needed.
- "26 green, 0 red" (the brief's expectation) is arithmetically unreachable while the T26
  expectFail stands: 26 results − 1 expected = "25 green, 1 red..." (gate.mjs:207-209), and
  DISCHARGING the declaration while the lane still fails flips the gate to FAIL. 25/1 IS the
  correct PASS for a tree where T26 is open-but-declared; 26/0 returns when T26's padding lands
  and the declaration is removed. Recorded as the honest result.
- .gate-15063.log/.gate-exit: PID-named transient harness files, not in .gitignore — removed at
  step 9/10 so the next verifier's step 1 sees 0.

### Step 8 — validations + metadata + scans (starts here)
### Step 8 — validations + backfills + scans
- 009 orchestor --strict (realpath'd, NODE_PRESERVE_SYMLINKS=1): `RESULT: PASSED`, exit 0. NOT
  stale — no rebuild (per instruction, had it been stale: STOP).
- backfill 009: exit 0, refreshed 1 / changed 0 (the leg's committed metadata already attests its
  docs). reviewFlags: prose_relationship_hints (advisory). NO drift, NO pruneCandidates.
- backfill 005 (parent): exit 0, changed 1 — re-derived 3 lines (last_save_at 20:18:54→21:25:42,
  source_fingerprint, handover.md hash) now attesting the REBASED handover.md (223's continuity +
  BOTH sections) — exactly the post-rebase re-derivation the rebase reserved.
- re-validate BOTH after: 009 `RESULT: PASSED` exit 0; 005(parent) `RESULT: PASSED` exit 0.
- scan-comments exit 0 (506 files, 0 violations of any kind); scan-failing-values exit 0 (413
  ticked / 69 phases, 266 with failing values, 147 against the recorded baseline — PASS, no newly
  ticked criterion without its failing value).
- Side effect of the GATE (step 7), disclosed: its last lanes re-stamped 7 of their own
  tools/live/*.json — diffs are ONLY measuredAt (every measured value identical);
  + parent graph-metadata.json (above). Plus 3 untracked task-transients:
  .gate-15063.log, .gate-exit, .pixel-delta-run1.json (deleted before step 9's commit).
### Step 9 — doc reconciliation + the landing commit
- Roadmap §5.1's 009 row (roadmap.md:810) already reads "**33%** — 2/6" — recount of
  009/goal.md §3 (lines 78-163): 2× `- [x]`, 4× `- [ ]` = 2/6 = 33%. NEITHER the leg nor this
  verification ticked a goal criterion (the leg's 24 files exclude goal.md; its work recorded as
  unticked tasks.md rows, T26 still open) → the row is CURRENT, no edit. Unticked rows verified
  against the no-tick rule: the four open criteria wait on the real-app probe / 000's receipt /
  the asking interface — the operator's-device row phrasing in the row's own prose stays honest.
- Removed the 3 task transients: .gate-15063.log, .gate-exit, .pixel-delta-run1.json.
- Committing: 7× tools/live/*.json (measuredAt only) + parent graph-metadata.json (re-derived)
  + .handover.md. NOT: 009's graph-metadata (backfill changed 0), no PNGs (0 moved), no
  screenshots/manifest (byte-identical), no main.js (byte-identical), no checklist (current).
