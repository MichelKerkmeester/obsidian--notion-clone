# Timeline View Teardown — working handover

## Task
Operator bug (2026-09-07 ~21:55): "if you open timeline view then go back to table
view, the timeline sits on top above table view for some reason and it glitches."

Main at 5e7f1426 (0.0.31; class prefix `obnotion-`).

## Plan
1. Reproduce LIVE in headless Chrome on the real DatabaseView host:
   open database -> switch to timeline -> switch back to table. Read DOM for
   leftover `.obnotion-timeline*`/gantt/calendar-timeline elements, overlapping
   absolutely/sticky positioned layers, leftover listeners, live
   ResizeObserver/rAF loops. Repeat at 402x874 phone profile and via embedded
   renderer. Record red numbers.
2. Root-cause in view-switch path: src/views/database-view.ts (~640, "last view
   type we finished rendering"), calendar-timeline-renderer.ts (~410 destroy()).
3. Fix at source so every renderer switch leaves zero elements/observers of the
   previous view.
4. Prove green with same live read. Add unit test in host's test file
   (switch timeline->table, assert no timeline nodes + destroy() ran; negative
   control by skipping teardown). Add permanent live pin in
   tools/live/render-assertions.mjs (view-switch residue = 0 for
   timeline->table and calendar->table).
5. Constructed capture pair of table after switch only if pixels change.
6. Roadmap §4 row mapped to owning packet (037-timeline-gantt-port or
   view-switch owner) with tasks/AC rows carrying the numbers. Never tick
   operator rows.
7. Verify: tsc --noEmit, vitest run, npm run build, render-assertions.mjs,
   sheet-grammar.mjs, npm run gate (foreground, </dev/null, timeout 600000,
   never backgrounded) - 26 green; recapture by decoded pixel delta; css-lane
   entries if styles.css changed; scan-comments/scan-failing-values exit 0.
8. Validate owning packet with realpath'd orchestrator --strict.
9. Commit with trailers (Co-Authored-By, Claude-Session). Do NOT push.

## Status log
- [2026-09-07] Worktree created: worktrees/225-timeline-view-teardown at
  .worktrees/225-timeline-view-teardown, branch tracks origin/main, HEAD
  5e7f1426. Symlinked .opencode, node_modules, specs/context.
- [2026-09-07] Root cause found: `src/views/rendered-view-roots.ts`'s
  VIEW_ROOT_CLASSES never named "pm-gantt-view" (the default timeline render's
  own root class — timelineLocalExtensions is opt-in/off), so switching away
  from timeline left it in the DOM (its position:sticky header pins over the
  next view). Neither host called the outgoing timeline/calendar renderer's
  destroy() on a switch either (only chart had that guard, in setViewType).
- [2026-09-07] Fixed: pm-gantt-view added to rendered-view-roots.ts and to
  embedded-database-renderer.ts's inline copy; both hosts (database-view.ts
  render(), embedded-database-renderer.ts renderResults()) gained
  teardownOutgoingViewRenderer(), called on every real view-type change;
  CalendarRenderer got a destroy() method mirroring CalendarTimelineRenderer's,
  wired into both hosts' unload paths.
- [2026-09-07] Live proof: new runViewSwitchResidueCheck in
  tools/live/render-assertion-harness.ts, wired as a permanent
  render-assertions.mjs lane. Red before fix: 1 leftover .pm-gantt-view after
  timeline->table (calendar->table already 0). Green after: 0/0. 3 new
  database-view.test.ts assertions (2 positive + negative control), confirmed
  failing pre-fix / passing post-fix.
- [2026-09-07] Full verification: npx tsc --noEmit clean; npx vitest run
  1644/1644 (153 files); npm run build exit 0; render-assertions.mjs exit 0;
  sheet-grammar.mjs exit 0; npm run gate 26/26 green (screenshots-fresh
  required `npm run screenshots` re-run after touching render-assertion-
  harness.ts/bundle.mjs; layoutHash/pixelHash unchanged for every affected
  capture; one unrelated PNG byte re-encode reverted).
- [2026-09-07] Docs: roadmap.md §4 row 67 added (owner 037-timeline-gantt-port);
  037's tasks.md Phase 9 T054, acceptance-criteria.md AC-008, spec.md REQ-008
  added. 037 and parent 005 both validate --strict, Errors: 0 (graph metadata
  refreshed via backfill-graph-metadata.js, realpath'd + NODE_PRESERVE_SYMLINKS=1
  — the symlinked .opencode/validate.sh combo silently no-ops otherwise).
  Packet handover entry appended to specs/005-component-surface-system/
  handover.md.
- [2026-09-07] Next: commit with trailers, do NOT push. Report HEAD SHA to
  orchestrator.

## Landing-verification log (dispatched successor, 2026-09-07)
- STEP 1 DONE: `git status --short | wc -l` = 1 (only untracked .handover.md —
  satisfies "0 apart from .handover.md"). `git log --oneline origin/main..HEAD`
  = exactly one leg commit: 4ba1b75e "fix(timeline): stop the outgoing view's
  DOM and observers surviving a switch". `git diff-tree --name-only -r 4ba1b75e
  | wc -l` = 24 files (claim said 24: CONFIRMED). 930 insertions, 630
  deletions. NOT yet rebased, NOT yet pushed. Successor: continue at STEP 2.
- STEP 2 DONE — claims read and statically verified:
  - C1 CONFIRMED: `pm-gantt-view` in BOTH lists — src/views/rendered-view-roots.ts:39
    (VIEW_ROOT_CLASSES, next to `obnotion-timeline` :33, with explanatory comment) and
    the inline copy in src/views/embedded-database-renderer.ts:1242-1243 (renderResults
    root-clearing selector, `.obnotion-timeline, .pm-gantt-view`).
  - C2 CONFIRMED: database-view.ts:7025-7026 `const viewTypeChanged =
    this.lastRenderedViewType !== viewType; if (viewTypeChanged)
    this.teardownOutgoingViewRenderer(this.lastRenderedViewType);` with the method at
    :7132-7135 calling destroy() on outgoing calendarTimelineRenderer (timeline) /
    calendarRenderer (calendar) / chartRenderer (chart). Twin in
    embedded-database-renderer.ts:1234-1235 (call) and :2388-2391 (definition).
    CalendarRenderer.destroy() NEW at src/views/calendar-renderer.ts:138.
  - C3 CONFIRMED: 3 it() blocks added to src/views/database-view.test.ts — 2 positive
    ("switching away from timeline calls the timeline renderer's destroy",
    "switching away from calendar calls the calendar renderer's destroy", both
    expect destroySpytoHaveBeenCalledTimes(1)) + 1 negative control ("negative
    control: re-rendering the same view type skips the teardown",
    expect(destroySpy).not.toHaveBeenCalled()).
  - C5 CONFIRMED (static): roadmap.md §4 row 67 (line 419) owner
    `037-timeline-gantt-port`, status "FIXED 2026-09-07, awaiting device read";
    037 tasks.md T054 (line 1003, ticked — leg's own task); acceptance-criteria.md
    AC-008 (line 63, "Met"; amendment note line 141); spec.md REQ-008 (line 167);
    parent handover.md entry (line 1318). Checkbox audit of the leg's diffs: 1
    `- [x]` added (T054 only), 0 in roadmap/AC — no operator/device row ticked.
  - C4 NOT yet re-derived (needs rebase first, steps 4-7).
  Successor: continue at STEP 3 (mutation tests).
- STEP 3 DONE — mutation tests:
  - (a) Deleted the `"pm-gantt-view",` entry from src/views/rendered-view-roots.ts
    (comment kept, MUTATION(3a) marker left in). `node tools/live/render-assertions.mjs`
    RED: exit 1, "render-assertions: FAIL — 1 assertion(s) failed" —
    `FAIL view-switch residue: timeline -> table — 1 leftover timeline root(s) after
    the switch (selector ".obnotion-timeline, .pm-gantt-view"); 1 mounted before the
    switch, table rendered: true`; the calendar scenario stayed PASS (0 leftover).
    COUNT PRINTED = 1 leftover — matches the leg's pre-fix claim. (Note: the lane
    itself runs at 1100x900; the 1200x900/402x874 figures in the leg's notes were its
    separate live debugging reads.) Restored: `git checkout --`, grep confirms the
    entry back (1 occurrence). Green re-proven later by the step 6/7 gate runs.
  - (b) Commented out the guard at database-view.ts:7026 (`if (viewTypeChanged)
    this.teardownOutgoingViewRenderer(this.lastRenderedViewType);` → MUTATION(3b)
    marker). `npx vitest run src/views/database-view.test.ts` RED: exit 1, exactly
    the 2 new positives failed ("switching away from timeline calls the timeline
    renderer's destroy", "switching away from calendar calls the calendar renderer's
    destroy"), 2 failed | 14 passed (16) — negative control still green. Exactly as
    claimed. Restored: `git checkout --`; re-run GREEN: 16 passed (16), exit 0.
  - Worktree back to clean (1 = .handover.md only) after both mutations.
  Successor: continue at STEP 4 (git fetch origin && git rebase origin/main; expect
  generated-artefact conflicts — take main, re-derive in step 6).
- STEP 4 DONE — fetch + rebase:
  - `git fetch origin` exit 0. origin/main moved +4: 3005e5bd (docs 221 landing),
    6f679e5e (chore(verify) re-derive 221 evidence onto 0.0.31), e795fc55 (feat
    tools-live host stylesheet model), c5b20ee4 (docs 005 operator state after
    0.0.31) — i.e. 221-live-host-model landed, exactly as anticipated.
  - First rebase attempt aborted: untracked .handover.md would be overwritten
    (221 committed their own root .handover.md). Kept ours at /tmp/handover-225-keep.md,
    removed, rebased, restored after.
  - `git rebase origin/main` conflicted in 8 files, ALL in the anticipated
    generated-artefact class: specs/005.../graph-metadata.json + 7x tools/live/*.json
    (capture-device-parity, renderer-coverage, replay, sheet-rebuild, sheet-teardown,
    touch-targets, unstyled-links). Verified ours/theirs semantics first: stage-2
    (ours) blob == origin/main blob (fd905aca...), so `git checkout --ours` = take
    main. Took main for all 8; 0 conflicts remained. Screenshots/manifest.json,
    handover.md, main.js, src/** auto-merged cleanly.
  - Parent handover.md auto-merge kept BOTH intents: 221's LANDED section (line 48)
    and our leg's entry (line 1482); 0 conflict markers. resumed-point frontmatter
    (line 3) still carries 221's wording — fine, our push will supersede after step 11.
  - `git rebase --continue` (GIT_EDITOR=true) exit 0. Leg now f56931f8 (16 files:
    24 minus the 8 taken-from-main). Status 1 (untracked .handover.md, restored).
  - The 8 conflicted artefacts now == main's versions; they are stale for OUR leg
    (manifest hash rows, live-lane JSONs, graph metadata) — MUST re-derive in STEP 6
    and re-run the whole gate in STEP 7. Successor: continue at STEP 5 (build, tsc,
    vitest).
- STEP 5 DONE — post-rebase full verification:
  - `npm run build` exit 0 (esbuild production). `npx tsc --noEmit` exit 0.
  - `npx vitest run` exit 0: **1645/1645, 153 files** (leg claimed 1644/1644 — the +1
    is main's: tools/lane/check-lane.test.mjs gained `it("parses a committed manifest
    bigger than the default pipe buffer")` in 221's landing (git diff
    5e7f1426..origin/main). No test lost; 153 files unchanged.
  Successor: continue at STEP 6 (screenshots x2, pixel-delta, re-derive stale
  artefacts).
- STEP 6 DONE — screenshots, pixel-delta, artefact re-derivation:
  - `npm run screenshots` run 1: exit 0 (608 captures). pixel-delta --run 1: 1 PNG
    moved — chrome-owned-menu-sheet-mobile-dark.png, changedPixels=2702, maxDelta=5.
  - run 2: exit 0 (608). pixel-delta --run 2: 1 PNG moved —
    calendar-empty-state-desktop-light.png, changedPixels=5, maxDelta=1.
  - Verdicts: calendar-empty-state-desktop-light.png = JITTER (moved in run 2 only,
    maxDelta 1 <= 12) -> `git checkout --` + patched its manifest bytes 44566->44567
    (head value). chrome-owned-menu-sheet-mobile-dark.png = moved in run 1 only but
    after run 2 the working PNG == HEAD blob (36715 == 36715 bytes) -> nothing to
    revert. board-view-desktop-dark.png: PNG never differed; its manifest row
    216975->216993 is a correction that now MATCHES the actual committed PNG (both
    216993 bytes) — HEAD's row (216975) disagreed with HEAD's own PNG. KEPT. The
    only remaining screenshots/ delta: exactly that 1-line manifest bytes fix.
    layoutHash/pixelHash EQUAL everywhere (script-verified: layoutEq/pixelEq true).
  - styles.css NOT changed (by our leg nor by the 221 rebase) -> NO acquire/edit/
    release css-lane triplet needed.
  - Stale-evidence re-derivation (the 4 the evidence lane named): ALL 4 owners
    re-run, all exit 0 — render-assertions.mjs (re-stamps renderer-coverage.json;
    ALSO re-proves the residue lane GREEN on the restored, post-rebase source:
    PASS timeline->table 0 leftover, PASS calendar->table 0 leftover — the leg's
    post-fix 0/0 re-derived), touch-targets.mjs, unstyled-links.mjs,
    capture-device-parity.mjs (run AFTER the manifest was final). Then
    `node tools/live/evidence.mjs --check-all` exit 0: "15 artefact(s) still
    describe this tree". replay/sheet-rebuild/sheet-teardown were already fresh
    (221's stamps still describe this tree).
  - build-operator-checklist.mjs: exit 0, rewrote 179 rows/62 phases, ZERO diff ->
    not stale, nothing to commit.
  - Working-tree note: the rebase made root .handover.md TRACKED (221 committed
    theirs; our copy = M). Per precedent each landing replaces it; ours will ride
    the final commit. Scratch: .pixel-delta-run{1,2}.json untracked (will be
    cleaned before the final report); .gate-*.log/.gate-exit to come.
  Successor: continue at STEP 7 (npm run gate, </dev/null, must print 26 green).
- STEP 7 DONE — the gate: exit 0, verdict line: "gate: PASS — 25 green, 1 red for a
  declared reason". The one red: sheet-grammar, declared by 009-live-verification
  (host-model button padding vs sort-panel obnotion-panel-button, ~10px right edge,
  Chrome+WebKit) — that is MAIN's own landed state: 221's landing note says "gate
  25+1 declared, T26 open". The instruction's "26 green, 0 red" wording predates
  221's landing (it anticipated 221 might land; it did). EXIT 0 = PASS; deviation
  documented, nothing to fix in our scope. NOTE: the gate's own lanes re-stamped
  replay.json, sheet-rebuild.json, sheet-teardown.json (now M, describing this
  tree) — legit, rides the final commit. Also: root scratch .gate-43323.log,
  .gate-exit, .pixel-delta-run{1,2}.json exist, NOT git-ignored — will be deleted
  before the final report (numbers preserved here). tracked diffs right now:
  .handover.md + screenshots/manifest.json + 7x tools/live/*.json. UNTRACKED diffs
  of 037/005 docs: none (all our doc changes came inside the replayed commit).
  Successor: continue at STEP 8 (validate 037 + parent, backfill both,
  scan-comments/scan-failing-values).
- STEP 8 DONE — validations:
  - 037 orchestrator --strict (realpath'd, NODE_PRESERVE_SYMLINKS=1): RESULT: PASSED
    (exit 0) — not stale, no rebuild needed.
  - 005 parent, same invocation, PRE-backfill: RESULT: FAILED (exit 1) — exactly one
    enforced violation: GENERATED_METADATA_INTEGRITY SOURCE_FINGERPRINT_MISMATCH
    (expected: the replayed commit's 005 doc edits (roadmap row 67, handover entry)
    vs the conflict-resolved parent graph-metadata.json that came from main).
  - backfill-graph-metadata.js --spec-folder 037: exit 0, refreshed 1, changed 0
    (the leg's own 037 graph-metadata already matched its docs); same for 005: exit
    0, refreshed 1, changed 1 (parent metadata now carries our fingerprints).
    reviewFlags: prose_relationship_hints on both (advisory). Then RE-validated:
    037 RESULT: PASSED (0), 005 RESULT: PASSED (0). 037/graph-metadata.json NOT
    modified by the backfill (0 changes); only 005/graph-metadata.json now M.
  - node tools/naming/scan-comments.mjs: exit 0 ("artifact-id violations: 0");
    node tools/naming/scan-failing-values.mjs: exit 0.
  Successor: continue at STEP 9 (reconcile roadmap §5.A row figure vs 037's
  goal.md criteria; commit everything with the trailers).
- STEP 9 DONE — §5.A reconciliation + working-tree state:
  - 037/goal.md criteria section = §3 (lines 72-248, before §4 LOG); checkbox
    census: 10 `- [x]` (lines 74, 79, 83, 87, 91, 96, 103, 128, 144, 154) + 10
    `- [ ]` (120, 123, 125, 127, 192, 194, 197, 199, 221, 231) = **10/20**. No
    checkboxes outside §3. The leg's work added NO criterion (AC-008/REQ-008/T054
    live in acceptance-criteria/spec/tasks), so the count is unchanged by the leg.
  - roadmap.md §5.A 037 row (line 846) reads "**50%** — 10/20" — ALREADY EQUAL to
    the recount. NO reconciliation edit needed. Unticked 221 (operator compares in
    vault) and 231 (operator rules on overpaint) correctly remain unticked — they
    name the operator; the leg's landing did not tick them (1 `- [x]` added by the
    leg: tasks.md T054 only). No roadmap/goal.md modification, so this step's diff
    = the re-derived artefacts + this .handover.md only.
  - Scratch cleanup BEFORE the commit: .gate-43323.log, .gate-exit,
    .pixel-delta-run1.json, .pixel-delta-run2.json deleted (their evidence numbers
    are recorded above). NOT git-ignored, hence removal, not leave-behind.
  - Commit: conventional, ends with BOTH trailers (Co-Authored-By: Claude Fable
    5.1 <noreply@anthropic.com> / Claude-Session: https://claude.ai/code/session_0189jHyPTPhrWEWyeFyaGc9Q).
  Successor: continue at STEP 10 (SPECKIT_SKIP_PREPUSH_ROUTE_GATE=1 git push origin
  HEAD:refs/heads/main; on rejection repeat steps 4-10, max 3), then STEP 11.
- STEP 10 DONE — push: `SPECKIT_SKIP_PREPUSH_ROUTE_GATE=1 git push origin
  HEAD:refs/heads/main` exit 0, FIRST attempt, no rejection: 3005e5bd..b6a0f847.
  `git log --oneline -1 origin/main` = b6a0f847 (the verifier commit). No retry
  needed; the 3-attempt budget untouched.
- STEP 11 DONE — packet handover entry + second commit:
  - specs/005-component-surface-system/handover.md: dated section added at the TOP
    of §1 (newest first, 221's shape): "2026-09-08 ~01:20, 225-timeline-view-teardown
    LANDED — verified, rebased onto 221's 3005e5bd, pushed to origin/main at
    b6a0f847", listing what was verified. 221's section kept BELOW it, untouched.
  - recent_action: "Verified and pushed 225-timeline-view-teardown to main at
    b6a0f847; gate 25+1 declared, T26 open" = EXACTLY 96 chars (limit 96). ALSO
    reconciled, per the completion-verification reconciliation duty + 221's
    3005e5bd precedent (the instruction named recent_action only): description
    resume point (225 landed; two legs remain in flight), last_updated_at/by,
    next_safe_action (the timeline fix no longer to-land), and the now-contradicted
    225 blocker line REMOVED (009 T26 / 069 / 067 blockers kept — all still true).
  - The handover edit stale-ed the parent's SOURCE_FINGERPRINT again (predicted);
    re-ran backfill 005 (changed: 1) and RE-VALIDATED BOTH: 005 PASSED, 037 PASSED.
    221's precedent: handover edit + re-derived metadata ride ONE commit (3005e5bd
    did exactly this) — this second commit does the same.
- FINAL STATE: origin/main = b6a0f847 + this second push; working tree clean (0);
  verifier DONE. Remaining legs for the next successor: 067 follow-up 3 (wt 222),
  069 (wt 224); 009 T26 still the gate's 1 declared red.
